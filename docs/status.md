# OpenCheck — Development History

OpenCheck has shipped through one hundred and twenty-six phases. The latest commit on `main` is the source of truth.

| Phase | Headline |
|------:|----------|
| 0 | Scaffold — FastAPI + React/Vite + 6 stub source adapters |
| 1 | Live UK Companies House + BODS v0.4 mapper + SSE streaming |
| 2 | Live GLEIF + OpenSanctions + OpenAleph + FtM/GLEIF mappers |
| 3 | Live Wikidata + EveryPolitician + reconciler + risk signals (incl. AMLA CDD RTS) |
| 4 | Cache-first dispatch + bods-dagre visualisation |
| 5 | Export endpoint (JSON / JSONL / ZIP) + OpenTender (DIGIWHIST) procurement source |
| 6 | LEI-anchored `/lookup` flow + BO design system + bods-dagre fix |
| 7 | BO design system applied to the frontend (Bitter / DM Sans / DM Mono, navy banner, card grid) |
| 8 | Acronyms spelled out, OpenAleph disabled, sources moved to a separate page, README refresh |
| 9 | Tooling fixes — `@vitejs/plugin-react` v5 / vite 8 alignment, README phase recap |
| 10 | Open Ownership processed BODS bundles for UK PSC + GLEIF as the canonical source |
| 11 | Cross-check related-party names against OpenSanctions + EveryPolitician — `RELATED_PEP` / `RELATED_SANCTIONED` |
| 12 | OO bundle as LEI lookup entry point + example LEI picker |
| 13 | `.env` loading from project root + BODS graph statement sanitiser + title/homepage link |
| 14 | bods-dagre `Invalid argument expected string` fix |
| 15 | Extraction script walks by `recordId` (not `statementId`) for correct subgraph extraction |
| 16 | OpenCorporates adapter (OCID-bridged via GLEIF) + BODS dagre relationship-edge fix + GODIN ribbon + Render deployment |
| 17 | FATF black/grey-list jurisdiction signals (`FATF_BLACK_LIST` / `FATF_GREY_LIST`) derived from BODS entity statements |
| 18 | OpenCorporates officer mapping (full BODS v0.4 interestType codelist), network relationship support, OC Relationships bulk-file infrastructure; GLEIF exception field-name fix (`reason` / `exceptionReason`); BODS validator completed to full 24-code codelist |
| 19 | BrightQuery / OpenData.org adapter — 185k+ US entities with LEIs; extraction + diagnostic scripts; entity-level risk signals shown on all source card headers |
| 20 | UI: risk signals visible on every source card header without clicking Go deeper; deepen panel de-duplicated |
| 21 | National corporate registry adapters — INPI (France, `fr_siren`), KvK (Netherlands, `nl_kvk`), Bolagsverket (Sweden, `se_org_number`), Zefix (Switzerland, `ch_uid`) — each with full BODS v0.4 officer mapping |
| 22 | ICIJ Offshore Leaks name cross-check — batched reconciliation API, `OFFSHORE_LEAKS` signals scoped to matching BODS statement; no API key required |
| 23 | Estonian e-Business Register (Ariregister) adapter — full open data: entity basics, shareholders, officers, beneficial owners, all mapped to BODS v0.4; national registry code emitted with `EE-RIK` scheme |
| 24 | SEC EDGAR Schedule 13D/13G adapter — major shareholders (>5 %) of US-listed companies from mandatory XML filings (December 2024 onward); no API key required |
| 25 | Brønnøysundregistrene (Norway) adapter — entity data and role-holders (CEO, board, officers) from the public Enhetsregisteret API; BODS v0.4 role mapping; NLOD 2.0; no API key required |
| 26 | Companies Registration Office Ireland (CRO) adapter — entity data from the CRO Open Data Portal CKAN API; CC BY 4.0; no API key required |
| 27 | PRH (Finland) adapter — entity data from the YTJ Open Data API; CC BY 4.0; no API key required |
| 28 | UR Latvia adapter — entity profiles, beneficial owners, officers, and shareholders from Latvia's Register of Enterprises via the CKAN Datastore API on data.gov.lv; open government data; no API key required |
| 29 | JAR Lithuania adapter — entity data from Lithuania's Register of Legal Entities (Registrų centras) via the public JAR HTML search interface; `lt_code` derived from GLEIF RA code `RA000430`; CC BY 4.0; no API key required |
| 30 | ARES (Czechia) adapter — entity data, shareholders, directors, and share capital from the Czech business register via the ARES REST API; `cz_ico` derived from GLEIF RA code `RA000163`; CC BY 4.0; no API key required |
| 31 | KRS Poland adapter — entity data and officer/board information from the Polish National Court Register via the KRS API; `pl_krs` derived from GLEIF RA code `RA000484`; public data; no API key required |
| 32 | Austrian Firmenbuch adapter — company name, address, status, and officers from the Austrian commercial register HVD SOAP API; `at_fn` (Firmenbuchnummer) derived from GLEIF RA code `RA000017`; CC BY 4.0; requires free `FIRMENBUCH_API_KEY` |
| 33 | RPO Slovakia adapter — entity data from Slovakia's Register of Legal Persons (Register právnických osôb) via the ŠÚ SR REST API; `sk_ico` (IČO) derived from GLEIF RA code `RA000526`; CC BY 4.0; no API key required |
| 34 | RPVS Slovakia adapter — beneficial ownership declarations from the Slovak Public Sector Partners Register (Register partnerov verejného sektora) via the Ministry of Justice OData API; covers entities supplying public bodies above statutory thresholds with verified KUV (konečný užívateľ výhod) disclosures; also triggered by `sk_ico`; CC BY 4.0; no API key required |
| 35 | BCE Belgium adapter — Belgian Crossroads Bank for Enterprises (BCE/KBO) — entity basics (name, status, juridical form, start date, registered address) from a local SQLite database built from the monthly KBO open data ZIP; `be_enterprise_number` derived from GLEIF RA code `RA000025`; supports name search via FTS5; KBO reuse licence; no API key required; activated via `BCE_BELGIUM_DB_FILE` |
| 36 | Corporations Canada (ISED) adapter — corporation records (name, status, act, registered address, business number, directors) for federally incorporated Canadian companies via the ISED API Gateway; `ca_corp_id` derived from GLEIF RA code `RA000072`; directors mapped to BODS v0.4 `seniorManagingOfficial` statements; OGL-Canada 2.0; requires `CORPORATIONS_CANADA_API_KEY` |
| 37 | ~~Ariregister (Estonia) rewritten to use the live SOAP/XML API (`ariregxmlv6.rik.ee`)~~ — **superseded by Phase 45**. Phase 37 introduced SOAP/X-Road credentials (`ARIREGISTER_USERNAME` / `ARIREGISTER_PASSWORD`) and SOAP envelope construction for the paid RIK data contract. Investigation in Phase 45 revealed that the contract type did not grant data access (all queries returned zero results). Phase 45 replaces this entirely with a credential-free public web scraper. |
| 38 | GLEIF: direct subsidiaries in BODS output — first page of Level 2 parent–child relationships emitted as child `entityStatement` + `relationshipStatement` (`appointmentOfBoard`, `beneficialOwnershipOrControl: false`); subsidiary count surfaced in UI below the GLEIF source card; Wikidata QID accuracy fix |
| 39 | Companies House: active directors → BODS — `officers.items[]` entries with a director role and no `resigned_on` are emitted as `personStatement` (knownPerson) + `relationshipStatement` (`seniorManagingOfficial`, `beneficialOwnershipOrControl: false`); officer ID extracted from `links.officer.appointments` for stable local IDs; secretary and resigned roles excluded |
| 40 | INPI France: non-BO individuals → BODS — `composition.pouvoirs[]` entries with `typeDePersonne == "INDIVIDU"` and `beneficiaireEffectif == false` are now mapped to person + relationship statements; full 65-code `roleEntreprise` codelist from INPI data dictionary embedded in mapper; external professional roles (auditors, liquidators, fiscal reps — codes 14, 71, 72, 77, 109, 150, 220) → `otherInfluenceOrControl`; all governance/management roles → `seniorManagingOfficial`; French label in `details`; `dateEffetRoleDeclarant` → `startDate`; `beneficiaireEffectif == true` entries silently skipped per Loi Sapin II / décret 2017-1094 |
| 41 | lib-cove-bods validation — `tests/test_bods_libcovebods.py` adds 23 tests running all mapper outputs through the BODS v0.4 JSON schema validator and additional quality checks (`libcovebods>=0.16`); fixes applied: `share.exclusiveMinimum` changed from boolean to numeric per JSON Schema Draft 2020-12; UK sub-regions (England/Scotland/Wales/Northern Ireland) mapped to `GB` in `_country_obj`; identifier scheme codes corrected to registered org.ids codes throughout (`NO-BRREG`→`NO-BRC`, `CZ-ARES`→`CZ-ICO`, `CH-UID`→`CH-FDJP`, `CH-ZEFIX`→`CH-COA`, `EE-ARIREGISTER`→`EE-KMKR`, `FR-SIREN`→`FR-INSEE`, `SG-UEN`→`SG-ACRA`, `OC-{jur}`→jurisdiction-specific org.ids code with `COA` fallback); GLEIF RA table France entry corrected (`RA000189` → `FR-INSEE`); corporate PSC `beneficialOwnershipOrControl` set to `false` when interested party is an entity (not a natural person) |
| 42 | CVR Denmark adapter — entity data from Denmark's Central Business Register (CVR) via the Datafordeler GraphQL API (`https://graphql.datafordeler.dk/CVR/v2`); `dk_cvr` derived from GLEIF RA code `RA000170` (Erhvervsstyrelsen); two-query fetch: (1) `CVR_Virksomhed` by `CVRNummer` → get `CVREnhedsId`, (2) batch-fetch `CVR_Navn` / `CVR_Adressering` / `CVR_Branche` / `CVR_Virksomhedsform` / `CVR_FuldtAnsvarligDeltagerRelation` by that ID; bitemporal data model (`virkningFra`/`virkningTil`) filtered Python-side to current records (`virkningTil = null`); natural persons excluded (CVRPerson requires MitID Erhverv + OAuth); entity statements only with `DK-CVR` identifier scheme; legal form from 50-code codelist; requires `CVR_DENMARK_API_KEY` (free from portal.datafordeler.dk) |
| 43 | BODS compliance audit (Phases 1–8) — eight-phase audit of the full mapper and validator surface area; 1738 backend tests pass (6 skipped, 5 xfailed). **Phase 1**: graph connectivity audit — shared `bods_validation_helpers.py` with `check_graph_connectivity()` / `check_interest_types()`; standalone `audit_bods_connectivity.py` script; graph connectivity assertions added to existing CH directors, Firmenbuch, GLEIF/FtM, INPI, and mapper tests. **Phase 2**: validator bug fixes — `known_ids` extended to include both `statementId` and `recordId` so canonical fixture cross-references are not flagged as dangling; inline unidentified-BO `interestedParty` (`{"reason": ...}`) correctly skipped; `test_bods_validator.py` (53 tests) covers all error branches, codelist values, `recordId`-based refs, legacy v0.3 wrapped formats, anonymous-person pattern. **Phase 3**: multi-source bundle assembly audit — `test_bods_multi_source.py` tests bundle assembly, statement deduplication, and cross-source reference integrity. **Phase 4**: per-adapter correctness matrix — `test_bods_correctness_matrix.py` parametrized across all mapper functions for entity type, jurisdiction, identifier presence, interest type validity. **Phase 5**: export endpoint audit — `test_bods_export.py` covers statement ordering, completeness, and schema validity from the `/export` endpoint. **Phase 6**: canonical fixtures conformance — `test_bods_fixtures_conformance.py` (46 tests) using the `bods-fixtures` pack (`core/01-direct-ownership`, `edge-cases/10-circular-ownership`, `edge-cases/11-anonymous-person`); all three canonical fixtures now pass `validate_shape()` with zero issues. **Phase 7**: statement metadata completeness — `_source_block()` fixed: 6 missing source names added (`acra_singapore`, `bce_belgium`, `jar_lithuania`, `prh`, `bods_gleif`, `bods_uk_psc`), 4 source types corrected from `thirdParty` → `officialRegister`; `test_bods_statement_metadata.py` (228 tests) parametrized across all 32 source IDs, plus `_stable_id`, publication details, and all three factory functions. **Phase 8**: date helpers + identifier schemes — `test_bods_date_helpers.py` (96 tests) for `_ee_date`, `_normalise_wikidata_date`, `_lv_date`, `_at_date_iso` covering all input formats, edge cases, and None/empty contracts; `test_bods_identifier_schemes.py` (184 tests) verifies non-empty identifiers, non-empty `id`/`scheme`, no duplicate schemes, and expected primary scheme code for all 16 national registry adapters (`GB-COH`, `NL-KVK`, `NO-BRC`, `IE-CRO`, `SE-BLV`, `LT-JAR`, `PL-KRS`, `SK-RPO`, `BE-BCE_KBO`, `CA-CORP`, `SG-ACRA`, `DK-CVR`, `AT-FB`, `LV-UR`, `CH-FDJP`, `FR-INSEE`), with per-adapter spot checks for optional secondary schemes (`XI-VAT`, `CA-BN`, `PL-NIP`, `PL-REGON`, `SK-OR`, `AT-UID`). |
| 44 | Cytoscape.js graph renderer — replaced `@openownership/bods-dagre` (45 npm downloads/week; required UMD global + SVGInjector XHR; BezierJS edge-offset bug causing invisible edges) with **Cytoscape.js** (7.9M downloads/week; MIT; academically published) + `cytoscape-dagre` plugin (same TB hierarchical layout). BOVS-compliant edge annotations declarative in the stylesheet: ownership interests → blue solid (`#1565c0`), control interests → orange solid (`#e65100`), role/management → purple dashed (`#6a1b9a`), with interest type + percentage label built from `interestLabel()` + `buildEdgeLabel()`. BOVS icon rendering: 9 entity/person SVG icons base64-encoded as data URIs in `src/lib/bovsIcons.ts` (bypasses Adobe Illustrator `xmlns:xlink` canvas taint bug). BOVS Metadata Overlays: jurisdiction flags rendered in a React HTML overlay layer (`position:absolute`, `pointerEvents:none`) above the Cytoscape canvas; a `cy.on('viewport')` listener recomputes screen-space positions on every pan/zoom; flag badge centred at 45° (NE) circumference per BOVS spec at `(cx + r·cos45°, cy − r·sin45°)`, size proportional to node radius (0.75r × 0.50r = 3:2 aspect ratio); flag fills badge via CSS `objectFit:cover`. BOVS icon centred in node at exactly 60% of rendered diameter via HTML overlay `<img>` — pixel-perfect at all zoom levels. Country flags: 265 SVGs committed to `frontend/public/bods-dagre-images/flags/`. `.gitignore` updated (removed stale exclusion). Backend and test suite unchanged: 1738 backend tests (6 skipped, 5 xfailed). |
| 45 | Two features shipped together: **(1) Ariregister rewritten as public web scraper** — supersedes the SOAP/X-Road approach from Phase 37. RIK confirmed (May 2026) that the paid API contract did not grant data-query access; all SOAP calls returned zero results. New implementation GETs `ariregister.rik.ee/eng/company/{reg}/company_print_json` (the public printable page) without credentials. HTML parser extracts: company name, legal form, status, registration date, address; officers table → mapped to Estonian role codes (`JUHL`, `PROK`, `LIKV`, etc.); shareholders table → person/entity type detected from code length (11-digit personal code = F, 8-digit registry code = J); beneficial owners table → control manner mapped to `O`/`K`/`H`/`M`. Output bundle format unchanged so `map_ariregister()` in `bods/mapper.py` is unmodified. `requires_api_key=False`, `live_available=True` always. Live-tested against Nordic Foods 1 OÜ (1 officer, 3 corporate shareholders, 2 BOs). **(2) BOVS risk signal overlays** on ownership diagrams — coloured pill badges at 315° NW circumference per BOVS Metadata Overlays spec: single signal → labelled pill (e.g. "S" for Sanctioned, "P" for PEP) in matching colour; multiple signals → "N ⚠" stack badge in worst-severity colour with shadow-circle depth indicator. Signal colours match `RiskChip.tsx` palette: rose (`SANCTIONED`, `RELATED_SANCTIONED`, `FATF_BLACK_LIST`), violet (`PEP`, `RELATED_PEP`), orange (`FATF_GREY_LIST`, `NON_EU_JURISDICTION`), etc. Backend: `statement_id` added to `SANCTIONED`/`PEP` signal evidence so the frontend resolves which BODS node to overlay. `buildSignalMap()` reads evidence fields from all signal types (`evidence.statement_id`, `evidence.subject_statement_id`, `evidence.matches[].statement_id`, `evidence.jurisdictions[].statement_id`, `evidence.longest_path[]`). Legend strip in diagram toolbar extended with Sanction/PEP/Jurisdiction swatches when signals are present. |
| 46 | Sudski registar (Croatia) adapter — entity data from the Croatian Court Register via the public `sudreg_javni` v3 JSON API (`https://sudreg-data.gov.hr/api/javni`); `hr_mbs` derived from GLEIF RA code `RA000156` (Croatian Court Registry), taken from the `registeredAs` field (zero-padded `potpuni_mbs`, e.g. `080000604`). OAuth2 client-credentials grant (token endpoint `…/api/oauth/token`, 6-hour TTL, cached in-process), then a single `GET /detalji_subjekta?tip_identifikatora=mbs&identifikator=<mbs>&expand_relations=true`. Maps legal name (`tvrtka.ime`), short name (`skracena_tvrtka.ime` → `alternateNames`), founding date (`datum_osnivanja`, ISO timestamp trimmed to date), registered seat (`sjediste`) and identifiers `HR-MBS` + `HR-OIB`; output validated against canonical lib-cove-bods v0.4 (zero schema errors). Officers/BO not exposed by the public API → entity statements only. Not-found/invalid identifiers return HTTP 400 with an `error_code` body → treated as stub. Wired into both `/lookup` and `/lookup-stream`. Requires `SUDREG_CLIENT_ID` / `SUDREG_CLIENT_SECRET` (free from sudreg-data.gov.hr; both end in literal `..`). `tests/test_sudreg_croatia.py` adds 25 tests. |
| 47 | Cyprus DRCOR adapter (committed, **inactive** — bulk-only) — company + officer data from the Department of Registrar of Companies and Intellectual Property via three monthly data.gov.cy CSVs (organisations, registered office, officials; no shareholders) under CC BY 4.0; `cy_he` derived from GLEIF RA code `RA000161` (DRCOR Companies Section), taken from the `registeredAs` field (Greek-script `ΗΕ` number normalised to digits). data.gov.cy exposes no query API — `/api/1/datastore/query` returns HTTP 404 and the ≈126 MB officials CSV is not datastore-imported — so the adapter follows the ACRA/BCE local-SQLite pattern: the CSVs are built into a local DB via `scripts/extract_cyprus.py` and queried by registration number; maps a company `entityStatement` + per-official person/entity statement + `seniorManagingOfficial` `relationshipStatement`. **Not in `REGISTRY` and not wired into the `/lookup` or `/lookup-stream` dispatch — never shown on `/sources`**; listed under [Inactive / bulk-only adapters](sources.md#inactive--bulk-only-adapters) alongside ACRA (Singapore) and BCE (Belgium). Activate via `CYPRUS_DRCOR_DB_FILE`. `tests/test_cyprus_drcor.py` adds 14 tests. |
| 48 | Australian Business Register (ABN Lookup) adapter — live, free adapter over the ABR ABN Lookup JSON web services (`abr.business.gov.au/json`), GUID auth via `ABN_GUID`. Unwraps JSONP callback wrapper client-side; routes by digit length (11 → `AbnDetails` endpoint, 9 → `AcnDetails` endpoint). GLEIF bridge uses two RA codes: `RA000014` (ASIC) → `au_acn` (9-digit ACN, normalised from GLEIF's spaced format) and `RA000013` (ABR/ATO) → `au_abn` (11-digit ABN). Entity-level firmographic data only — ABN, ACN, entity name and type, ABN/GST status, registered state and postcode, and business (trading) names — mapped to a single BODS entity statement with `AU-ABN` + `AU-ACN` identifiers; trading names emitted as `alternateNames`; state + postcode as partial registered address; `entityType.subtype` populated from the `EntityTypeName` field (e.g. "Commonwealth Government Entity"). Cancelled ABNs (`AbnStatus != "Active"`) annotated with `dissolutionDate`. Wired into both `/lookup` and `/lookup-stream`. License: CC BY 3.0 AU (Australian Taxation Office). Requires a free `ABN_GUID` from [abr.business.gov.au/Documentation/WebServiceRegistration](https://abr.business.gov.au/Documentation/WebServiceRegistration). `tests/test_abr_australia.py` adds 14 tests. |
| 49 | Graph-native demo — curated BODS subgraph pipeline, Neo4j CSV export, slide viewer, licensing |
| 50 | National ID search — three-tab search panel (Company name \| National ID \| Paste an LEI). Users enter a local registration number and select a country; `searchByNationalId()` queries all three GLEIF registration-ID filter fields in parallel (`filter[entity.registeredAs]`, `filter[registration.validatedAs]`, `filter[registration.otherValidationAuthorities.validatedAs]`), each scoped to the registration authority RA code to prevent false matches from coincidental ID collisions. Covers all 17 countries with active adapters: AT, BE, CA, HR, DK, EE, FR, IE, LV, LT, NL, NO, PL, SG, SK, SE, GB. Single result → auto-navigate to `/lookup-stream`; multiple → picker; zero → amber "not found" notice with "try by name" fallback. Per-country format validation is advisory and non-blocking: amber border + inline hint fires after `onBlur` only (`nationalIdTouched` state), never preventing submission since GLEIF may store IDs in a normalised form. Two new files: `frontend/src/lib/raCodes.ts` (RA codes, labels, placeholders, format regexes, `COUNTRY_OPTIONS`, `validateNationalId()`) and `frontend/src/lib/gleifNationalId.ts` (`GleifSearchResult`, `searchByNationalId()`). Pure frontend change — no backend routes added or modified. Commit `9f4e79b`. |
| 51 | Backend & frontend hardening — five architecture improvements from a Claude Fable 5 review of a codebase built to this point with Sonnet and Opus. **(1) One lookup pipeline** (`7282b7a`): `/lookup` and `/lookup-stream` were two hand-synchronised copies (~600–700 duplicated lines) — the source of the Corporations Canada regression `603c086`. A single async generator `_lookup_pipeline()` now resolves the GLEIF anchor, builds derived identifiers, dispatches adapters, streams results, reconciles, deepens and merges risk; the stream serialises its events, the sync endpoint collects them (2,403 → 1,159 lines). Flushed out three latent bugs: duplicate live-mode fetches on the sync path, double-appended deepen pairs on the stream, and wrong Norway/Sweden RA codes in the notes (now `RA000472` brreg, `RA000544` bolagsverket, verified live). **(2) Self-describing adapters** (`86a5a3e`): each register adapter declares its lookup wiring as class attributes (`lookup_derivers`, `lookup_dispatch_keys`, `lookup_pass_legal_name`); `routers/lookup.py` builds its dispatch tables from the registry at import and raises if an adapter declares lookup keys without a `_bh_<id>()` hit builder. The `_MAPPERS` dict and the 60-line import wall are gone — mappers resolve by the `map_<source_id>` convention. Hand-maintained expected-source lists in `test_sources.py`/`test_app.py` replaced by filesystem discovery (`_DELIBERATELY_UNREGISTERED` allowlist for bulk-only adapters). **(3) Shareable & survivable lookups** (`e3058ce`): in-memory replay cache for completed runs (15-min TTL, keyed `LEI:deepen_top`) so refresh/shared-URL/reconnect is near-instant; `?refresh=true` bypasses; new `GET /lookup-source?lei=&source_id=` re-runs one source. Frontend: lookups addressable via `?lei=` (pushState + popstate), a stream drop after the anchor resolves keeps partial results behind a "Resume lookup" banner, and failed source cards get a "Retry source" button. **(4) Cold-start & tail-latency** (`ca4a6c1`): the FastAPI lifespan now warms the GEM/GLEIF/GEOT indexes in a background thread (`climatetrace.warm_caches()`, non-fatal on failure) and the lazy index builds run via `asyncio.to_thread` so they never block the event loop; every adapter declares a `lookup_timeout_s` wall-clock budget (default 30 s; cvr_denmark 90 s, openaleph 60 s) enforced with `asyncio.wait_for`, emitting `source_error` with `error_type: "timeout"`. **(5) Code-split bundle** (`c76b89c`): `BODSGraph` (Cytoscape + cytoscape-dagre) lazy-loaded via `React.lazy`/`Suspense` since it only renders on "Visualise" — main bundle 777 kB → 271 kB (244 → 80 kB gzip), ~67% cut to first paint; unused `zustand` dependency removed. New `tests/test_lookup_pipeline.py` pins sync/stream parity, the wiring invariants, replay-cache semantics and per-source budgets. |
| 52 | GEM Global Energy Ownership Tracker (GEOT) project-level ESG data — expands the Climate TRACE / GEM source beyond emissions to ownership reach. **(1) climatetrace adapter fixes** (`3373c31`): `_parse_parents()` now strips the ownership-share suffix GEM embeds in both parent columns (`E100000000817 [55.0%]` — ~12,000 of ~13,600 populated rows previously left the bracket in the parsed entity ID) and surfaces the share as a float, which the BODS mapper emits as `share.exact`; data download switches GCS-bucket-first (the GitHub `ownership.zip` lagged the Climate TRACE bucket — Feb vs May 2026 — with the zip kept as offline/test fallback); a cycle-safe ownership summary walks the entity→entity graph to count direct vs group-wide Climate TRACE assets, subsidiaries and a sector breakdown. **(2) Per-LEI project portfolio** (`2f1e9d2`): because the GEOT xlsx sits behind a reCAPTCHA download form, `scripts/build_geot_projects.py` condenses each release's nine per-tracker ownership-closure sheets (coal/gas/bioenergy plants, coal/iron mines, gas & oil/NGL pipelines, steel, cement) into the committed artifact `backend/opencheck/data/geot_projects.json.gz` (99 KB, 10,734 entities from 246k closure rows). Counting recipe handles the per-sheet column quirks (coal's `Owner GEM Entity ID` vs others' `Parent GEM Entity ID`, iron's `Operating status`, the per-tracker asset-ID columns), dedupes exact-duplicate and "timepoint" rows, sums distinct paths to a unit capped at 100 %, takes the max share across a project's units (≥50 % = controlled), and assigns each project its best status (operating > development > mothballed > retired > cancelled). The ESG card is redesigned into a two-stat header — emissions (linked to Climate TRACE) alongside "N live projects · X operating · Y ≥50 % controlled" (linked to the GEM Ownership Tracker) — above a per-tracker table with live/operating/≥50 % columns and proportional bars, a mothballed/retired/cancelled footnote, a "group reach" line, and parent pills that now show ownership shares. `ATTRIBUTIONS.md` cites the GEOT release per GEM's recommended wording. Rerunning for a new release is one command (`--release "Jul 2026"`). |
| 53 | AI summaries — a grounded, source-cited narrative of an entity for a customer-due-diligence / financial-crime audience; every statement is backed by OpenCheck's own data, enforced mechanically rather than by prompt wording alone. **(1) Offline core** (`4fa5296`): new `opencheck/narrative/` package — `build_evidence_packet()` distils a `/lookup` (or `/report`) result into an `EvidencePacket` of atomic, already-evidenced **facts** (each with source name, adapter `source_id`, BODS statement ids and a confidence derived from source authority — `officialRegister` → high, else medium), structured **risk items**, **sources consulted** (with licence) and **gaps** (CDD-relevant absences); `summarise()` calls Claude (`claude-sonnet-4-6`, structured tool output, temperature 0) for one executive paragraph plus per-claim citations referencing evidence ids (`f`/`r`/`g`); `validate_narrative()` drops any claim citing an unknown or empty id and withholds the paragraph on violation, so "no unprovable information" is guaranteed in code. Absence is treated as evidence — clean entities carry a synthesised "the risk engine ran and found nothing" fact and every gap is itself a citable item, so the model can state "no beneficial owner was disclosed" or "no risks were found" *with* a citation rather than fabricating one. Compliance-analyst tone: risks framed as structural/jurisdictional indicators for review (never determinations of wrongdoing), confidence stated per signal, name-matches and incomplete screening caveated, internal signal codes kept out of the prose. Tooling: versioned prompt (`PROMPT_VERSION`), six synthetic golden packets (`tests/golden_narrative/`, no PII — clean, sanctioned related party, no-PSC gap, name-matched/sparse, complex ownership layers, errored source) and an offline eval harness (`scripts/eval_narrative.py`) scoring a machine-checkable rubric (grounded / names subject / flags name-match / surfaces risks / surfaces gaps / no raw codes / length) for iterating wording before any UI. Gated on `ANTHROPIC_API_KEY`; model overridable via `OPENCHECK_NARRATIVE_MODEL`; `anthropic` added as a dependency. **(2) Endpoint + UI** (`ee3ec58`): `GET /narrative?lei=` reuses the same cached lookup pipeline as `/lookup` (so the summary can never diverge from the page), builds the packet, runs `summarise` off the event loop via `asyncio.to_thread`, validates, and returns the summary, surviving claims, limitations and the full packet (so the UI can resolve cited ids back to evidence). Flag- and key-gated (`404` when `OPENCHECK_NARRATIVE_ENABLED=false`, `503` when no key); no token streaming by design — grounding can only be checked once the whole structured answer exists, so streaming raw tokens would put unvalidated text on screen. Facts and risks carry the adapter `source_id` (via a REGISTRY name→id map) for reliable source-card linking. Frontend: an on-demand `NarrativePanel` at the top of the entity result page — a "Generate summary" button (no model call until asked), the paragraph, an Evidence list with per-claim citation chips (labelled by source, with a confidence dot), a limitations block, and a disclaimer naming the model + prompt version; clicking a chip scrolls to and flashes the originating source card (`oc-cite-flash`) and emits an `oc:cite` event the BODS graph listens for, expanding and highlighting the cited statement node. `tests/test_narrative.py` and `tests/test_narrative_endpoint.py` added. |
| 54 | Accessible PDF export — a take-away, source-cited due-diligence report for any entity, generated from the live OpenCheck profile and rendered as a **tagged PDF/UA-1** so it meets WCAG. Every line is attributed to its source — the same "nothing unprovable" rule as the narrative. New `backend/opencheck/reporting/` package: **`diagram.py`** renders each source's BODS relationships as a purpose-built print **SVG** in the BOVS visual style (green person / navy entity glyphs, ownership = blue edge, control/role = purple edge, the interest described on every edge, longest-path layering for chains, `<title>`/`<desc>` for non-visual access) — chosen over screenshotting the Cytoscape canvas, whose BOVS icons/flags/risk badges live in an HTML overlay that `cy.png()` misses; **`html_report.py`** assembles the report HTML from a `LookupResponse` — entity title, identifiers table, live-check callout with a `segno` QR to `opencheck.world/?lei=`, the grounded AI summary embedded **only when already generated on the page** (no model call on download), deterministic risk signals, per-source findings with licence chips, one diagram + text-equivalent table per source, and licensing/attribution from `assess_licensing()`; **`pdf.py`** renders to a tagged PDF via WeasyPrint (lazy import; `PdfUnavailable` → 503). Accessibility follows the WCAG 2.0 PDF techniques: tagged structure tree (`Marked`/`StructTreeRoot`), `lang`, document title shown in the viewer, heading bookmarks, `th scope` table markup, figure alternatives (each graph carries a `desc` **and** a data table), running header/footer with page numbers, and body text darkened to ~7:1 contrast. Backend: **`POST /export/pdf`** rebuilds from the same cached lookup pipeline as `/lookup` (so the PDF can't diverge from the page), renders off the event loop via `asyncio.to_thread`, and streams the file with a `Content-Disposition` filename; the client-supplied narrative is embedded verbatim and diagrams are server-rendered, so no graph images are uploaded. Frontend: a "Download PDF" button in the summary-panel header (always available) via `api.downloadReportPdf()`, sending the generated narrative from state when present. Deployment: `weasyprint`/`segno` added as runtime deps (+ `pikepdf` for the tagged-PDF render test), with Pango (`libpango-1.0-0`, `libpangoft2-1.0-0`) and `fonts-dejavu-core` added to the backend Docker image (WeasyPrint 53+ renders via Pango, no Cairo). A4. `tests/test_reporting_diagram.py`, `tests/test_reporting_html.py` (incl. a live tagged-PDF render assertion) and `tests/test_export_pdf_endpoint.py` added. |
| 55 | Malta Business Registry (MBR) adapter — OpenCheck's **30th source** and **22nd national register**, and the first surfaced from a systematic sweep of the EU's High-Value Datasets on `data.europa.eu`. Live, key-less adapter over the **MBR Open Data API** (`openapi.baros.mbr.mt`, an EU Open Data Directive HVD): `mt_crn` (Maltese registration number — a short letter prefix + digits, e.g. `C 113927`) derived from GLEIF RA code `RA000443` (Registry of Companies), taken from the `registeredAs` field and normalised to the canonical `"<prefix> <digits>"` form GLEIF and the API share. A single `GET /api/v1/companies/{registration_number}` returns core entity data — name, legal form (`type`), status (`state`), registered office, area of activity, registration number and date — mapped to one BODS v0.4 `entityStatement` (`MT-MBR` identifier scheme, legal form carried in `entityType.details`, MT jurisdiction). **Entity data only**: the MBR's separate beneficial-ownership register is not part of this API, and the list endpoint has no name filter (cursor pagination only), so the source is entered via the LEI lookup flow and `search()` returns `[]` (identifier-keyed). No API key and no auth (confirmed against the OpenAPI spec — no security schemes; an unauthenticated request returns 200). Wired into both `/lookup` and `/lookup-stream` purely by the self-describing-adapter conventions (`lookup_derivers` on the class + `_bh_malta_mbr()` hit builder + `map_malta_mbr()` resolved by name), and into the **National ID search** frontend (`raCodes.ts`: MT → `RA000443`, `C 113927` placeholder, advisory non-blocking format check). License: CC BY 4.0 (Malta Business Registry). `tests/test_malta_mbr.py` adds 18 tests; an opt-in live smoke test (`tests/test_live_smoke.py`, run with `pytest --run-live -m live`) fetches a real MBR record and asserts it maps to valid BODS — doubling as a production access check that fails loudly if the endpoint ever blocks non-browser clients. Live-verified against Blue Lagoon Holding Limited (`C 113927`). Commits `2abb410` (adapter) and `8a46252` (live smoke test + source count → 30). |
| 56 | Brazil — Receita Federal CNPJ register adapter — OpenCheck's **31st source**, **23rd national register**, and the first with a real ownership graph from open data outside the EU. Live, **key-less, two-tier** adapter: **OpenCNPJ** (`api.opencnpj.org`, primary — a fast static serving of the Receita Federal CNPJ open dataset) with a **BrasilAPI** (`brasilapi.com.br`) fallback on 404/error; a provider-agnostic `_normalise()` collapses either field shape (human-readable labels vs numeric type codes) into one bundle. `br_cnpj` (14-digit CNPJ) derived from GLEIF RA code `RA000681` (Receita Federal), normalised from the punctuated `registeredAs` (`33.000.167/0001-01`). Beyond the company entity statement (`BR-RFB` identifier, legal nature in `entityType.details`, founding date, registered address, BR jurisdiction), it maps the **QSA** (Quadro de Sócios e Administradores): each sócio becomes a **personStatement** (natural person / foreign) or **entityStatement** (legal-entity partner, carrying *its own* CNPJ as a cross-source identifier) plus an **ownership-or-control** relationship — owner-type qualifications (sócio / acionista / quotista / titular) → `shareholding`, administrators/directors → `seniorManagingOfficial`, `data_entrada_sociedade` → `startDate`. Brazil being non-EU, the `NON_EU_JURISDICTION` risk signal fires. No API key (both providers return 200 unauthenticated). Identifier-keyed (no name search), entered via the LEI flow; wired into `/lookup` + `/lookup-stream` by the self-describing-adapter conventions and into the **National ID search** frontend (`raCodes.ts`: BR → `RA000681`, CNPJ). License: Receita Federal CNPJ open data. `tests/test_cnpj_brazil.py` adds 17 tests (both provider shapes, OpenCNPJ→BrasilAPI fallback, QSA → person/entity/relationship mapping); an opt-in live smoke test fetches a real CNPJ and asserts valid BODS with relationships. Live-verified against PETROLEO BRASILEIRO S.A. — PETROBRAS (`33000167000101`, 9 QSA members). Commit `25aeb41`. |
| 57 | Securities (ISINs) & sanctioned-securities overlay — a new entity-level **Securities** panel (below the source cards, above ESG) showing the securities mapped to an LEI and surfacing any that are sanctioned, plus a first-class `SANCTIONED_SECURITY` risk chip. **Three open datasets, three roles:** **GLEIF** `/lei-records/{lei}/isins` (authoritative LEI→ISIN list + total count — fetched as count + one page only; major issuers carry tens of thousands, e.g. Deutsche Bank ≈ 22,500), **OpenFIGI** `/v3/mapping` (security type / name / ticker / exchange, on **only** the ISINs actually shown; optional `OPENFIGI_API_KEY` raises the rate limit), and the free **OpenSanctions `securities.csv`** export (the sanctioned subset by LEI + regimes, incl. EO 14071 investment bans — covering GLEIF's blind spot: Rosneft has **0** ISINs in GLEIF but **12** sanctioned). Lazy **`GET /securities?lei=&page=`** assembles them off the main lookup pipeline and **never enumerates every ISIN** — the sanctioned subset (small, pre-filtered) comes from the index, the long tail is a GLEIF count behind a page. Frontend `SecuritiesSection`: a sanctions-first red banner showing the first two sanctioned ISINs with company-level regime chips (shown once in the header, capped at four + "more") and a "Show all N" toggle, then a count + a searchable, type-filtered "Browse all" drawer; CC-BY-NC notice on the sanctioned data; mobile-safe (ISIN + type rows; regimes never repeated per row). **Risk:** `sanctioned_securities_signal()` injects a high-confidence `SANCTIONED_SECURITY` into the single lookup pipeline (in-memory index lookup, no network; structural dedup) so it appears in the risk chips, `/export` and the MCP `lookup` tool; rose chip in `RiskChip.tsx`. **Index pipeline:** `scripts/extract_securities.py` turns the 8.8 MB company-centric export into a compact `lei→{isins, regimes}` JSON (≈**187 LEIs / 13,050 ISINs** — most sanctioned companies are private with no LEI/ISINs; `ext_*` reference datasets excluded from regimes), loaded from `OPENCHECK_SECURITIES_INDEX_FILE` **or** `OPENCHECK_SECURITIES_INDEX_URL` (warmed at startup), `.gitignore`d (CC-BY-NC, hosted as a GitHub Release asset). A weekly GitHub Action (`refresh-securities-index.yml`) rebuilds, sanity-checks (≥50 LEIs) and republishes the asset, optionally triggering a Render redeploy via `RENDER_DEPLOY_HOOK_URL`. `tests/test_securities.py` (10 tests: assembly, sanctioned overlay, Rosneft zero-GLEIF case, file & URL loading, the extractor, the risk signal); `docs/securities.md`. Live-verified against Rosneft (`253400JT3MQWNDKMJE44`) and Sberbank (`549300WE6TAF5EEWQS81`). Commits `abd42a4`, `e427fbf`, `0db0799`, `83fb825`, `ca38d20`. |
| 58 | Time Machine — change-over-time timelines (GLEIF + Companies House) — surfaces the **temporal half of BODS v0.4** that almost no demo shows (`statementDate`, the stable `recordId` + `recordStatus` new/updated/closed lifecycle, interest `startDate`/`endDate`), across **two sources on one axis**. **Insight that shaped it:** both GLEIF and Companies House expose a live, per-entity *typed change stream* — GLEIF's field-level modification log (`/lei-records/{lei}/field-modifications`, `modificationType` + `valueOld`/`valueNew` for LEI **and** RR records) and CH filing history (typed categories: PSC01–09, change-of-name, reregistration, …) — so no snapshot-diffing or hoarding is needed and detection reduces to a per-source allowlist into one shared model. **The noise is the product:** Heineken's three-year GLEIF log is 100 % administrative (annual renewals, `LastUpdateDate`, timezone/category backfills) — a GLEIF `NextRenewalDate` renewal is the twin of a Companies House CS01 "confirmed, no change" — so filtering it *is* the feature. New `backend/opencheck/timeline/` package: **`model.py`** — the source-agnostic, **raw-first** `ChangeEvent` (always carries the faithful registry change; notability is a derived `tier` and suppression happens in the renderer, never by dropping events) + a controlled `ChangeType` codelist (Tier 1 ownership/control: `OWNER_ADDED`/`OWNER_REMOVED`, `PARENT_CHANGED`, `REPORTING_EXCEPTION_CHANGED`, `CONTROL_*`; Tier 2 identity/status: `LEGAL_NAME_CHANGE`, `LEGAL_FORM_CHANGE`, `STATUS_CHANGED`, `SUCCESSION`, `JURISDICTION_CHANGE`, `REGISTRATION_RETIRED`, `ADDRESS_CHANGE`; Tier 3 admin noise) each mapped to a BODS `recordStatus`. **`gleif.py`** + **`companies_house.py`** — the two emitters, each an allowlist into the codelist, with two guards proven by a **live dry-run on Wm Morrison Supermarkets** (the 2021 Clayton, Dubilier & Rice take-private): (1) **legal-form encoding backfills are not real changes** — only flag a change when the ELF form *class* changes (PLC `B6ES` → private-limited `H0PO` counts; the `8888`→`B6ES` re-encode is suppressed); (2) **interest dates come from the relationship period, not the publish date** — GLEIF first *recorded* the Market Bidco parent in Nov 2023 but the relationship period shows consolidation began Nov 2021, so the BODS interest `startDate` uses the economic date and the publish date stays provenance. **`assemble.py`** — merges the streams into one timeline: cross-source **entity** changes are de-duplicated (the PLC→Limited rename collapses to one row carrying *both* GLEIF and Companies House, the CH *effective* date winning over GLEIF's *recorded* date), while **ownership** changes are deliberately kept separate (GLEIF reports corporate parents by LEI; CH reports PSCs, often individuals with no LEI — no reliable cross-source key). **`service.py`** + **`routers/history.py`** — lazy **`GET /history?lei=&include_noise=`** (never on the main lookup): GLEIF key-free, Companies House via a dedicated `COMPANIES_HOUSE_HISTORY_API_KEY` (falls back to the lookup key), graceful degradation to GLEIF-only, never 500s. **Honest dates throughout:** every event carries `date_basis` (`effective` / `recorded` / `snapshot_window`) + `date_confidence`, so GLEIF and CH precision are never rendered as equivalent. Frontend: a quiet **"See timeline"** button on the GLEIF and Companies House source cards (top-right, under the result count) opens a **vertical timeline rail** below the card — tier-coloured dots (teal ownership, blue identity), the value transition, parent LEI + economic interest dates for ownership changes, GLEIF/Companies House source chips linking back to `search.gleif.org` and the CH filing-history page, and a "show administrative changes (N)" toggle that folds the Tier-3 noise back onto the same rail. `docs/time-machine.md` records the design; `tests/test_timeline_*.py` add 37 backend tests (codelist + both emitters + assembler + endpoint) and `HistoryTimeline.test.ts` adds 8 frontend tests. |
| 59 | New Zealand — Companies Register / NZBN adapter — OpenCheck's **32nd source** and **24th national register**, and one of the richest: a real ownership graph *with shareholder percentages*. Live, key-gated adapter over the New Zealand Business Number (NZBN) API (`api.business.govt.nz/gateway/nzbn/v5`, Companies Office / MBIE). `nz_company_number` derived from GLEIF RA code `RA000466` (Companies Register) — note GLEIF stores the **company number** under `registeredAs`, *not* the NZBN, so the adapter resolves it first: the NZBN directory search (`EntitiesGet`, whose `search-term` explicitly covers "legacy numbers (eg company number)") maps the company number → NZBN, then `EntitiesByNzbnGet` returns the **FullEntity**. That one response is unusually complete and maps to a full BODS v0.4 graph: the company entityStatement (identifiers `NZ-NZBN` + `NZ-COH`, registered address, trading and previous names → `alternateNames`); **directors** (`roles[]`) → person/entity + `seniorManagingOfficial` relationships with appointment/cessation dates; **shareholders** (`company-details.shareholding.shareAllocation[]`) → person/entity + `shareholding` relationships carrying **`share.exact`** (each allocation's shares ÷ the company total), with legal-entity shareholders carrying their own NZBN / company number as cross-source identifiers; and the **ultimate holding company** → an `otherInfluenceOrControl` (indirect) relationship. New Zealand being non-EU, the `NON_EU_JURISDICTION` risk signal fires. Auth is a free `NZBN_API_KEY` sent as the `Ocp-Apim-Subscription-Key` header (no OAuth for the read operations). License: CC BY 4.0 (NZ Companies Register / NZBN, MBIE). Wired into `/lookup` + `/lookup-stream` by the self-describing-adapter conventions and into the **National ID search** frontend (`raCodes.ts`: NZ → `RA000466`). `tests/test_nz_companies.py` adds adapter + mapper tests (company-number → NZBN resolution, directors / shareholders / UHC, `share.exact` percentages, BODS `validate_shape` clean); a key-gated opt-in live smoke test (`--run-live`, skips without `NZBN_API_KEY`) fetches Fonterra Co-op (`1166320`) from the real API. |
| 60 | GLEIF subsidiary network — direct **and** ultimate children, the whole-group view — a lazy, panel-only reveal on the GLEIF source that complements the existing inward parent/direct-child relationships with the **outward** network. **Why both relationship sets:** GLEIF Level 2 publishes two accounting-consolidation relationships per parent/child — *direct* (the subject directly consolidates the child) and *ultimate* (the group head ultimately consolidates it, possibly several layers down) — and the two sets overlap. New `subsidiaries.py` service fetches both endpoints (`/lei-records/{lei}/direct-children` and `/ultimate-children`), merges by child LEI and tags each child `direct`, `ultimate` or `both`; **counts are exact** (from GLEIF's pagination `total`) even when the child list is page-capped (10×100 per relation). New `map_gleif_subsidiaries()` emits the subject + one entityStatement per distinct child + **one relationshipStatement per relation** — so a `both` child carries **two** distinct statements (`directOrIndirect` `direct` and `indirect`), kept faithful in the data and the export. **The rationalised graph:** rather than draw the `both` case as two edges (or hide one), `bodsToGraph` (`frontend/src/lib/bodsGraph.ts`) now keeps the same-pair direct+ultimate edges and **merges them into one edge annotated "Controls (direct + ultimate)"**, while still suppressing the genuinely redundant *skip-level* ultimate edges the direct tree already implies (`consolidationKind()` extended to recognise the live mapper's detail form; clean-ups B/C reconciled). **The readability threshold:** `node_estimate = max(direct_total, ultimate_total, distinct)` — **≤ 150 nodes** renders in the interactive Cytoscape graph (lazy-loaded), **above** that degrades to a table (direct children first, then the indirect ultimate-only tail) + a **Download BODS** button so a large group can be rendered in the user's own tooling. Lazy **`GET /subsidiaries?lei=&format=`** (`format=bods` adds the statements), gated on `OPENCHECK_ALLOW_LIVE`, **never on the main lookup**; no API key (GLEIF is CC0). Frontend `SubsidiaryNetwork` panel on the GLEIF card: invitation → summary (direct/ultimate/distinct counts, indirect-only, jurisdiction spread) → graph or table + export, each fetched only when asked. `tests/test_subsidiaries.py` adds 7 backend tests (a `both` child → two distinct statements, the graph/table threshold, indirect-only, gating, endpoint LEI validation); 3 `bodsGraph.test.ts` cases pin the merged-edge annotation; `docs/subsidiary-network.md` records the design. Commit `5af1504`. |
| 61 | New Zealand director/shareholder associations — recall fix (address upgrades, doesn't gate) — a tuning change to the Phase 59 NZ enrichment after live use surfaced **zero** associations for any company. The Companies Entity Role Search API is keyed on a **name string** (no stable person id), so the panel had tiered every name match by **address** — `pafId` exact / overlapping address lines / name-only — and counted **only** the address-corroborated subset, hiding name-only matches as "weaker, not counted". In practice that gate suppressed essentially every real association: a **career director** (the trigger case was Fonterra's Holly Kramer, who sits on many boards) files a *different* address on each board — home, a service address, the company's registered office — so almost every genuine match landed in name-only and vanished, and recall collapsed to ~zero. **Fix:** address is now an *upgrade*, not a gate. `summarise_person()` counts **every** name match and grades each company by the strongest tier seen — `high` (same `pafId`), `medium` (overlapping address lines, Jaccard ≥ 0.6), `low` (name matches, no address corroboration — "may be a different person who shares the name"). New per-person fields `address_match_count` (high + medium) and `name_only_count` (low) replace `weaker_count`; people are ranked address-matched-first then most-connected. Frontend (`NzAssociations.tsx`): name-only matches shown in amber, the per-person line reads "Also in N other companies (X address-matched, Y name-only)", and the caveat is reworded; the honesty rails are unchanged — subject company excluded, ceased directorships skipped, the per-name register `totalResults` still flags common names ("N records under this name — only a sample checked"), and nothing is asserted as a determination. Backend response schema (`routers/nz_associations.py`) and `docs/nz-associations.md` updated. `tests/test_nz_associations.py`: the two tiering tests rewritten to the new counting, plus a regression test reproducing the career-director case (differing addresses → three name-only matches surfaced, not dropped). Commit `fe794b6`. |
| 62 | Wikidata controlling-owner extraction + `STATE_CONTROLLED` signal — foundation / family / state ownership from Wikidata, mapped to BODS. **Origin:** an investigation of the community **Wikidata MCP** (`wd-mcp.wmcloud.org`) concluded the MCP is a *transport over the same Wikidata* OpenCheck already queries — it unlocks no new data and would only add a third-party dependency to the deterministic backend; the value is in *what* we extract. Live probes showed Wikidata carries rich **foundation/family ownership** for big private firms (Robert Bosch → Robert Bosch Stiftung 92% + family fiduciary vehicles; Koch → the Koch brothers; Heineken → Heineken Holding + the family) and **state ownership** for famous SOEs (Gazprom, Rosneft, Equinor, EDF, CNPC), with **82% of ownership statements carrying a reference** — often authoritative (company filings, the Danish CVR register, government press). Coverage is famous-names-only, so the enrichment is **presence-only and corroborating** (its absence means nothing) — like the Offshore Leaks signal. **What shipped (three commits):** (1) a new `_OWNERSHIP_QUERY` over `P127`/`P749` (statement-node-per-owner, carrying the `P1107` proportion + references `P248`/`P854`/`P813`) plus `_classify_owner` / `_parse_ownership` in `sources/wikidata.py` — each owner categorised (person / foundation / arrangement / company / GLIE / state / stateBody), the share normalised as **indicative**, references deduped, and **family-typed owners dropped** (a family is neither a legal entity nor a person — not fabricated). (2) Wired into the live `fetch()` (a third concurrent SPARQL query) + `map_wikidata()`, which emits each owner as the correct BODS shape — `personStatement` (BO true), `registeredEntity` (foundation/company/GLIE), `arrangement` (trust/Treuhand), or `state`/`stateBody` — per the BODS [Representing state-owned enterprises](https://standard.openownership.org/en/0.4.0/standard/modelling/repr-state-owned-enterprises.html) requirement, with the indicative `share.exact` and the underlying reference as the relationship source. (3) A new **`STATE_CONTROLLED`** risk signal (`risk.py`): BODS-derived and source-agnostic, it fires `medium` when a controlling owner is a `state`/`stateBody` entity, evidence-linked to the state node + the controlled entity; orange "State-controlled" chip + "St" graph badge; **not** part of the AMLA composite, never a determination. `docs/wikidata-ownership.md` records the design + the BODS modelling table; `docs/risk-signals.md` adds the signal. `tests/test_wikidata_ownership.py` (5), `tests/test_bods_wikidata.py` (3 new), `tests/test_risk_state_controlled.py` (5). Commits `a2b4c79`, `d68506c`, `af9feef`. |
| 63 | Time Machine — Denmark (CVR) emitter, the **5th source** — change-over-time events from the Danish Central Business Register, and the cheapest emitter to date. **Key insight:** CVR (Datafordeler) is *bitemporal* — every name / address / legal-form / status / branche record carries a `virkningFra` / `virkningTil` validity period — and the existing `cvr_denmark` adapter already fetches the **full** history (its GraphQL queries select the validity fields with no point-in-time filter); it just collapsed to the current row at the summary step (`_current()` → `virkningTil is null`). So the emitter reconstructs change events from the bundle's `_raw_*` blocks with **no new API calls**, and because `virkningFra` is a real *effective* date (validity time, not when the registry recorded it), CVR events are `DateBasis.EFFECTIVE` / `DateConfidence.HIGH` — the same high-quality dates as Companies House and New Zealand, unlike GLEIF's *recorded* dates. **What shipped:** (1) `sources/cvr_denmark.py` now preserves the full virksomhed node list as `_raw_virksomhed` (was discarded), so status / lifecycle history is derivable. (2) New `timeline/cvr_denmark.py` — `cvr_change_events(bundle)` maps the bitemporal blocks to `LEGAL_NAME_CHANGE` (sekvens-0 legal names only — secondary/trade names excluded), `LEGAL_FORM_CHANGE`, `STATUS_CHANGED` (active → bankruptcy / dissolved), and `ADDRESS_CHANGE` (registered seat); `branche` (industry) recodes are kept raw-first as Tier-3 admin noise; no-op re-registrations (same value, new period) are skipped; `deltager` relations are skipped (enhedsId-only — CVRPerson is access-restricted, so there is no readable name). (3) `timeline/service.py` derives `dk_cvr` from the GLEIF record (RA000170) and reconstructs events via the normal CVR fetch, gated on `CVR_DENMARK_API_KEY`, best-effort (never sinks the timeline). (4) Frontend: `cvr_denmark` added to `TIMELINE_SOURCES`, so the "See timeline" button shows on the CVR card. `docs/time-machine.md` gains an "Emitters (current)" table covering all five. `tests/test_timeline_cvr.py` adds 6 tests, anchored on the real Novo Nordisk history (Novo Industri A/S → Novo Nordisk A/S, 1989, incl. secondary-name exclusion). Commit `e09092f`. |
| 64 | Subsidiary network in the main export (opt-in) — the GLEIF subsidiary network (Phase 60) was a separate lazy view (`/subsidiaries?format=bods`), so a compliance user exporting "the BODS for this entity" via `/export` silently got the entity but **not its group**. A defensible performance default — a large group can be hundreds of entities — but it was an accident of the build rather than a conscious choice. Now it is opt-in: **`GET /export?lei=&subsidiaries=true`** folds the subsidiary network into the bundle for *every* format (JSON / JSONL / XML / ZIP); off by default. `_merge_subsidiaries()` (`routers/export.py`) deduplicates by `statementId` — the subsidiary subject shares the GLEIF subject's id (both derive from the LEI via `_gleif_entity_statement`), so it collapses to a single subject and the child relationships resolve against it cleanly — re-validates the merged bundle, and never mutates the cached `/lookup` payload (returns a `model_copy`). The ZIP manifest records `subsidiary_network_included` + `subsidiary_statement_count`; the Export panel exposes a checkbox. Gated on `OPENCHECK_ALLOW_LIVE`; bounded by the subsidiaries service's existing page cap, so a massive group is truncated (the manifest count reflects what was included). `docs/subsidiary-network.md`, the `/api` export card and the how-it-works export row updated. `tests/test_export_endpoint.py` adds 3 tests (opt-in merge + statementId dedup, default-off never invokes the fetch, manifest flags). Commit `e3c5f28`. |
| 65 | Auto-generated changelog page (`opencheck.world/changelog`) — a public "what's new" page generated from this development-history table, so the table is the single editorial gate: an update appears on the changelog only if it earned a phase row here. **How it's hooked up:** build-time, no runtime dependency. A `prebuild` npm hook (`frontend/scripts/gen-changelog.mjs`) copies `docs/status.md` into a bundled JSON on every build — Render runs `npm install && npm run build`, so the changelog **auto-refreshes on every deploy** (which is exactly when a new phase row ships). Chosen over a runtime GitHub fetch or a backend endpoint so the page loads instantly and can't be taken down by a backend cold-start. The parser (`frontend/src/lib/changelog.ts`, unit-tested) splits each row into `{ phase, title, summary, commits[] }` — the **title** is the lead clause before the em-dash, the **summary** is the first sentence (tolerant of decimals like `v0.4`, abbreviations like `e.g.`, escaped pipes and inline markdown), and **commit hashes** are pulled from the trailing ``Commit(s) `…` `` clause and linked to GitHub. `ChangelogPage.tsx` renders a newest-first card list reusing the existing design-system tokens (`oo-ink`/`oo-muted`/`oo-rule`, `font-head`, `rounded-oo`, `tracking-oo-eyebrow`), with phase chips and commit-link chips; `/changelog` is a route + footer nav link, deep-linkable via the existing SPA rewrite. `frontend/src/lib/changelog.test.ts` adds 6 parser tests (row→entry, title/first-sentence split, decimal + abbreviation handling, single/multiple commit extraction, no-commit rows, commit URL). Commit `a9efc30`. |
| 66 | Senzing JSON export format — project the assembled BODS v0.4 bundle into the [Senzing entity specification](https://www.senzing.com/docs/entity_specification/) so an OpenCheck ownership graph loads straight into Senzing for entity resolution. **The mapping:** a new pure `bods/senzing.py` (`map_to_senzing()` / `to_senzing_jsonl()`) targets the *modern* `FEATURES`-array shape — each BODS entity becomes a `RECORD_TYPE: ORGANIZATION` record, each person a `PERSON`, and both carry exactly one `REL_ANCHOR` keyed by their BODS `statementId`. Every disclosed ownership/control relationship is folded into a `REL_POINTER` on the **interested party's** record, aimed at the subject's anchor — read as "owner →(`OWNER_OF 50%`)→ company" — with `REL_POINTER_ROLE` derived from the BODS interest type (`shareholding`→`OWNER_OF`, `votingRights`→`VOTING_RIGHTS_IN`, …, falling back to the upper-cased type so a new interest type is never lossy) plus a rendered share band, and `FROM`/`THRU` dates from the interest. Identifiers map to `LEI_NUMBER` (by scheme or the 20-char LEI shape) or `NATIONAL_ID` with the issuing country inferred from `GB-COH`-style scheme prefixes; an `unspecified`/unknown interested party is dropped (no record to anchor to). **Why it lines up cleanly:** OpenCheck sets `statementId == recordId` for entity/person statements and relationships reference those ids, so the BODS id is a stable Senzing `RECORD_ID` and the anchor/pointer wiring resolves with no extra entity resolution. **Surfaces:** `GET /export?format=senzing` returns newline-delimited Senzing JSON (`…-senzing.jsonl`); `senzing.jsonl` is added to the ZIP bundle (+ a `senzing_record_count` in the manifest); and the `opencheck_export_bods` MCP tool accepts `format='senzing'`. Direction follows Senzing's standard owner→subject convention (the reference [BODS-0.2 mapper](https://github.com/Senzing/mapper-open-ownership) points the other way). `tests/test_senzing.py` adds 8 unit tests (entity/person/relationship mapping, anchor/pointer wiring, share-band roles, LEI detection, unspecified-party drop, JSONL shape); export + MCP integration tests cover the new format and the ZIP inclusion. README, `docs/how-it-works.md` and `docs/mcp-server.md` updated. Commit `d42f5a7`. |
| 67 | FullCheck mode — enhanced due diligence over the wider corporate network — a QuickCheck/FullCheck mode toggle on the results page: QuickCheck stays the fast subject screening, FullCheck maps and screens the network connected to it. Built in three phases on the live progressive-discovery traversal engine, all behind the toggle (the QuickCheck path is untouched). **Phase 1 — eager network build:** a "Run FullCheck" control expands a single unified ownership graph (`FullCheckPanel` fetches the subject's merged BODS, lazy-loaded) to a visible **depth budget** via a client-side loop over `/expand-layer` — no new backend, node cap 150, with progress + cancel. **Phase 2 — risk-first:** each hop's sub-lookup already screens the expanded entity, so `/expand-layer` now also returns those **risk signals** (evidence statement-ids remapped onto the anchor); the UI accumulates network-wide risk, overlays it on the graph, and shows the **QuickCheck-vs-FullCheck diff** ("QuickCheck flagged X on the subject; FullCheck surfaced N more across the network"). **Phase 3 — export the network:** `POST /export-network` formats the browser-assembled network as BODS (JSON/JSONL/XML), Senzing JSON (per-record `DATA_LICENSE`/`ATTRIBUTION`), a lightweight Neo4j/Cypher projection (`bods/neo4j.py` `to_cypher()` — `bods-neo4j` remains the full-fidelity path), or a ZIP bundling all of them + manifest + `LICENSES.md`. Decisions (from the FullCheck v0.2 spec): mode toggle, foreground job, visible depth budget, compliance-EDD risk-first. `tests/test_expand.py` + `tests/test_export_network.py` cover signal propagation, the Cypher projection, and every export format. Commits `ab6fe0f`, `4506603`, `330f102`. |
| 68 | FullCheck network reconciliation + source provenance — the unified FullCheck graph overlaid every source's view of a company as separate nodes (the same subject appearing 2–4 times), so this adds a display-time **entity-resolution** pass and surfaces where each node's data comes from. `frontend/src/lib/reconcile.ts` (`reconcileBods`) merges entity statements that share a strong identifier into one canonical node via union-find, keyed by a stable identifier-derived id, remapping every reference and de-duplicating relationships; `remapSignals` rewrites risk-signal evidence ids so the BOVS overlays still land on the merged node. It merges on an **LEI** (scheme-agnostic) or a **national registration number scoped by jurisdiction** — sources disagree on the scheme label for the same register (Novo Nordisk's Danish company no. 24256790 arrives as scheme `""` from GLEIF, `DK-COA` from OpenCorporates and `DK-CVR` from CVR, with no shared LEI on the CVR copy), so a bare reg. number also keys by `JUR:<code>:<value>` (scoped to national-register schemes so QCC/S&P/BIC/OpenCorporates ids never cross-merge, and same-value ids in different jurisdictions stay distinct). It never merges on name alone — name-only matches are deferred to a future `POSSIBLY_SAME_AS` confidence edge. Provenance: `GraphNode.sources`/`GraphEdge.sources` carry the contributing sources (pooled across merged edges); a `SourceLegend` strip lists "sources in this network" with per-source node counts + a corroboration tally, and each chip toggles a **highlight** that spotlights that source's nodes/edges and dims the rest (highlight, never hide) via a new `highlightSource` prop on `BODSGraph`. Reconciliation is **display-only** — the frontier/expansion bookkeeping keeps running on the raw statements, so the live traversal is untouched and QuickCheck panels render raw. `frontend/src/lib/reconcile.test.ts` covers the merge, relationship de-dup, the cross-scheme/jurisdiction case and the cross-jurisdiction guard; verified against live Novo Nordisk data (four subject records → one node, four sources). Commits `d29b355`, `f212cbe`. |
| 69 | OECD-UNSD MEIP signpost source — a new *kind* of source that is deliberately **not mapped to BODS** and contributes nothing to the ownership graph. The OECD-UNSD [Multinational Enterprise Information Platform](https://www.oecd.org/en/data/dashboards/oecd-unsd-multinational-enterprise-information-platform.html) — a joint OECD / UN Statistics Division initiative — publishes an annual "Global Register" of the world's 500 largest MNEs and their **126,000+ subsidiaries**; ~30,000 of those subsidiaries (and 482 of the 500 MNE heads) carry an LEI, which is the only join OpenCheck's LEI-anchored flow needs. When the subject LEI is in the register, a **signpost card** renders at the very bottom of the results page — beneath the data-source cards and the ESG box — proving the match and pointing the user to the OECD site to download / reuse the full dataset. **Two match modes** (the LEI key sets are disjoint): *subsidiary* shows the entity's immediate parent and its ultimate parent MNE; *MNE-head* shows "one of the world's 500 largest MNEs" plus a subsidiary count. **Corroboration:** the identifiers MEIP publishes (OpenCorporates, S&P Capital IQ) are cross-checked against the identifiers GLEIF already publishes for that LEI (`ocid` / `spglobal`) and flagged where they agree, so the card doubles as a light cross-source trust signal — the LEI itself is always corroborated (it is the subject's own GLEIF-verified LEI), Refinitiv PermID is informational, and the `DUNL` column is dropped (it only routes to the S&P Capital IQ id already captured). **How it's built:** static annual data, vendored, no live API. `backend/scripts/build_meip.py` condenses the ~127k-row CSV into two LEI-keyed JSON tables (`data/meip_subsidiaries.json`, 29,487 entries; `data/meip_mne_heads.json`, 482); `opencheck/meip.py` (`meip_lookup(lei, known_ids)`) returns a Pydantic `MeipMatch` with a `mode` discriminator + per-identifier corroboration flags; the lookup pipeline emits a `meip` SSE event straight after the GLEIF anchor (symmetric to `possibly_same_entities`), captured into `LookupResponse.meip`. Frontend: `MeipSignpost.tsx` renders the card (subsidiary name → OECD dashboard, deep-linked / corroboration-flagged identifier pills, "download & reuse on OECD.org" CTA) at the bottom of the results page, beneath the CDD source cards and the `EsgPanel`. Documented as a **non-BODS "signpost source"** in `docs/sources.md` (excluded from the active-adapter count) and `ATTRIBUTIONS.md` (OECD Terms and Conditions, attribution required). `tests/test_meip.py` (unit: both match modes, corroboration flag logic, no-match) + two `tests/test_lookup_pipeline.py` parity tests (the `meip` event fires and stream ⇄ sync agree; absent for a non-register LEI). Commits `9383b92`, `2d8e877`, `36fdd35`. |
| 70 | EITI adapter — company-level **payments to governments** as a new ESG source. The [EITI API](https://eiti.org/api) (v2.0, OAS spec at `/openapi/rest?_format=json`) covers 65 implementing countries: 81k organisation rows (54.6k companies, 90% with a national `identification`), revenue rows with GFS classification and USD normalisation. **No LEIs in the data** — but identifications are national registry/tax numbers (verified: GB Companies House incl. `SC`/`FC` prefixes, NO 9-digit orgnr, NL KvK-adjacent, US EINs), so the adapter keys on the GLEIF anchor's **`(jurisdiction, registeredAs)`** pair — the first OpenCheck source to use `registeredAs` globally across all jurisdictions rather than via per-country RA derivers. The API's documented `identification` filter is **not implemented server-side** (nonsense values return the unfiltered set; to be reported to data@eiti.org), so matching is local: `scripts/build_eiti_index.py` crawls the full organisation table (~1,650 pages, resumable via `--from-dir`) into the committed artifact `opencheck/data/eiti_organisations.json.gz` (246 KB, 6,197 country+identification keys across 56 countries; leading-zero/punctuation-insensitive matching). Payment rows come live from `/api/v2.0/revenue?organisation={id}` — the one server-side filter verified to work — for up to the 4 most recent reporting years, aggregated per year and per GFS stream. Verified end-to-end: Equinor UK (gb_coh `01285743`, derivable from its LEI) → 4 reporting years (2018–2021) → licence fees + OGA levy totals. Hit identifiers use the verified-format map (GB→`gb_coh`, NO→`no_orgnr`, NL→`kvk_number`; EITI independently publishes these numbers, so reconciler corroboration is legitimate) else neutral `eiti_identification`. `map_eiti()` emits the entity statement with the national identifier (GB-COH / NO-BRC / NL-KVK schemes where verified); payments are fiscal flows, not ownership — no relationship statements. Frontend: `EitiCard` in the ESG panel — payments headline, GFS stream bars, reporting-years line, EITI attribution. EITI is GODIN-aligned and its data strategy commits to BODS: when EITI publishes BODS-native company/SOE data, `map_eiti()` is the consumption point. 14 new tests in `tests/test_eiti.py`. Commit `7534400`. |
| 71 | Wikirate adapter + ESG summary tiles — open, community-researched **ESG metric answers** as a new ESG source ([Wikirate](https://wikirate.org/), CC BY 4.0, GODIN member). LEI-keyed with a Wikidata-QID fallback via the nested `filter[company_identifier[value]]` param — the flat filter shape 500s server-side, and Wikirate's Cloudflare bot protection blocks anonymous server-to-server requests, so the adapter requires `WIKIRATE_API_KEY` (the `X-API-Key` header bypasses the challenge; both verified live 2026-07-07). Company cards independently publish LEI, Wikidata, Companies House and SEC CIK identifiers, asserted on the hit for cross-source corroboration. The card shows the total data-point count plus the latest answer per metric and links out to wikirate.org for the full record. With three ESG sources the panel got too tall, so it now opens as one compact summary tile per source — each naming its data origin and headline stat — with full cards expanding on click; every ESG card carries an explicit "Data from <org> · <licence>" tag. Verified end-to-end with BP plc (2,290 data points). Commit `b5525ba`. |
| 72 | Shareable og:image cards — sharing a lookup now produces a link whose social-media preview is a **live summary card** for the entity instead of the generic brand image: entity name, risk-signal count and the first three signal chips in the on-site RiskChip colours, on the split-panel brand design. Rendered server-side with Pillow (1200×630, drawn at 2× and downsampled; Bitter + DM Sans bundled under SIL OFL) at `GET /og/{lei}.png`, fed by the lookup replay cache with a GLEIF-name teaser variant when no completed run is cached. Because social crawlers don't execute JavaScript, the static SPA can't vary its meta tags per `?lei=` URL — the new crawler-readable `GET /share/{lei}` page carries per-entity Open Graph/Twitter tags and instantly redirects humans to the frontend; the subject card gains a "Copy share link" button that hands out that URL. Commit `b8ff0c9`. |
| 73 | Replay provenance badging — a lookup served from the 15-minute replay cache (a page refresh, a shared `?lei=` URL, an SSE reconnect) is now **labelled** instead of silently passing as live, restoring the original project plan's "live vs cached" honesty commitment (§5.5) for the cache that replaced the demo corpus. Backend: replay-cache entries carry the wall-clock UTC completion time of the original run alongside the monotonic TTL clock (`_ReplayEntry` named tuple); a replayed `/lookup-stream` run is **prefixed** with a new `replayed` SSE event (`{fetched_at, age_seconds}`, emitted before any result so the UI knows up front), and the sync `/lookup` response mirrors it as `replayed: bool` + `fetched_at` — fresh runs and `?refresh=true` runs never carry either. Frontend: the subject card shows an amber provenance badge on replayed results — "Results from a check run N min ago — not re-queried" — with a **"Run a fresh check"** action wired through `streamLookup`'s existing (previously unused) `refresh` parameter to `?refresh=true`. The share card's replay-cache scan updated for the new entry shape. Tests: the replay-cache pipeline tests pin the provenance fields and the replayed-event prefix (payloads otherwise identical to the fresh run), `test_share.py` seeds the named-tuple shape, and `SubjectCard.test.ts` covers the age label. Commit `2776f0e`. |
| 74 | FollowTheMoney (FtM) export format — the export the original project plan (§7.6) promised ("take data into OpenSanctions / Aleph investigative workflows") finally ships: **`format=ftm`** on `GET /export` and `POST /export-network`, `ftm.jsonl` in both ZIP bundles (+ `ftm_entity_count` in the manifests), `opencheck_export_bods(format="ftm")` on the MCP server, and an FtM option in the Export panel and the FullCheck network export menu. New pure mapper `bods/ftm.py` (`map_to_ftm()` / `to_ftm_jsonl()`), mirroring `bods/senzing.py`: one FtM entity per BODS entity/person statement — the BODS `statementId` is the FtM id, so references resolve with no extra entity resolution — schema-routed `registeredEntity` → `Company`, `state`/`stateBody` → `PublicBody`, else `LegalEntity`; identifiers map to `leiCode` / `wikidataId` / `registrationNumber` (`idNumber` for persons), countries lowercased per the FtM country type. Each disclosed interest becomes its own interval entity: management interests (`seniorManagingOfficial` / `boardMember` / `boardChair`) → **`Directorship`**, everything else → **`Ownership`** carrying `percentage` (exact value or band label like `>25-50`) and direct/indirect `ownershipType`; a relationship with no interests → `UnknownLink`; unspecified/dangling interested parties are dropped, never fabricated. Nodes are always emitted before links so streaming loaders (`alephclient write-entities`, the `ftm` CLI) resolve references first pass. Every property emitted was verified against the followthemoney schema YAMLs (LegalEntity, Person, Ownership, Directorship, UnknownLink). Distinct from `opencheck/ftm.py`, which converts only the lookup subject for OpenAleph `POST /api/2/match`; [bods-ftm](https://github.com/StephenAbbott/bods-ftm) remains the canonical bidirectional converter — this covers the subset OpenCheck emits with no ICU/followthemoney toolchain at runtime. `tests/test_ftm_export.py` adds 12 mapper tests plus `/export`, ZIP, network and MCP format coverage. Commit `d9826aa`. |
| 75 | Per-IP rate limiting / abuse protection for the public API — the server-side rate limiter the original plan promised for live calls (plan.md §8.4 / Phase-5 checklist) finally ships. The public API had no throttle: anyone scripting `/lookup` or `/lookup-stream` could burn the quota of every key-gated upstream (Companies House, NZBN, Corporations Canada, CVR, Wikirate, OpenSanctions…) and the CPU/Anthropic-token cost of `/export/pdf` and `/narrative`; the replay cache absorbs repeats, not hostile volume. Now [slowapi](https://slowapi.readthedocs.io/) with **in-memory storage** (one Render instance — no Redis, no new infrastructure; a `storage_uri` swap upgrades to shared counters if the API ever scales out) enforces three per-IP tiers, all env-tunable: **lookup** 10/min (`/lookup`, `/lookup-stream`, `/search`, `/stream`, `/report`, `/export` — the full fan-out endpoints that dispatch every source adapter), **heavy** 3/min (`/narrative`, `/export/pdf`, `/export-network`), **default** 60/min (everything else public); `/health`, `/sources` and `/mcp` deliberately unlimited, `OPENCHECK_RATE_LIMIT_ENABLED=0` kills it. Client IP behind Render's Cloudflare-fronted proxy resolves `True-Client-IP` → first `X-Forwarded-For` entry → socket peer (`request.client.host` alone would rate-limit the proxy, i.e. everyone at once). Deliberately **no `SlowAPIMiddleware`** — it is a `BaseHTTPMiddleware`, which interferes with SSE streaming and crashes on the mounted MCP routes — instead every route carries an explicit `@limiter.limit(<tier>)` decorator and a coverage test pins that any new public route must pick a tier or document an exemption. 429s speak the API's `{"detail": …}` error shape with `Retry-After` + `X-RateLimit-*` headers; errors inside handlers still spend budget (garbage traffic is exactly the abuse this guards). Endpoints reused in-process (MCP tools, `/narrative`, `/export`, layer expansion) split into undecorated `_lookup_impl` / `_search_impl` / `_resolve_national_id_impl` plus thin rate-limited routes, so internal calls never trip the limiter. 12 new tests in `tests/test_rate_limit.py`; the suite-wide conftest disables the limiter so existing tests never trip budgets. Commit `530cbf5`. |
| 76 | Markdown report export — the due-diligence report as portable text. `POST /export/markdown` renders the same report the PDF pipeline assembles — same cached lookup result, same embedded AI summary, claims, analyst dispositions and stored-dispositions fallback (factored into a shared `_resolve_dispositions()`) — as plain CommonMark via a new `reporting/markdown_report.py` that mirrors `html_report.py` section by section (cover → identifiers → live check → summary/claims/gaps → risk signals → per-source findings → ownership tables → licensing). Two deliberate divergences from the PDF: the BOVS diagrams are replaced by their **text-equivalent relationship tables** (the same rows `source_diagram` computes — wikis, git and LLM pipelines get more from the table than an image), and the QR code by the plain live-check URL. **No WeasyPrint dependency** — the route is always available, including on deployments where `/export/pdf` 503s, making Markdown the report's built-in fallback format. Frontend: `downloadReportMarkdown()` in `api.ts` (sharing a `downloadReport()` helper with the PDF path), wired into the Export control in the next phase. `tests/test_reporting_markdown.py` (sections, claims + disposition badges, packet-derived gaps, pipe escaping, stub-only reports) + `tests/test_export_markdown_endpoint.py` mirroring the PDF endpoint suite. Commit `a9ec470`. |
| 77 | Export menu + "Download data" rebrand — the prime top-right slot on the AI summary card stops advertising only PDF. The single "Download PDF" button becomes one grouped **Export** menu: **Report** items download (PDF / the Phase-76 Markdown report — both post the card's narrative + analyst dispositions, which is why the control stays on this card), and the **Data** item is an honest jump link ("Download data — Jump to the data section") with a ↓ glyph rather than a fake download — mixing a navigation verb into a download menu is the design's main risk, so the item is styled as a link and says where it goes. `ExportMenu.tsx` renders a dropdown on `sm+` and a bottom sheet with a scrim below `sm` (44px rows); menu a11y is full: `aria-haspopup`/`aria-expanded`, `role=menu/menuitem`, roving Arrow/Home/End focus via a pure `nextMenuIndex()` helper (unit-tested), Escape closes and returns focus to the trigger, click-outside and Tab close. The results-page section is rebranded **"Download BODS bundle" → "Download data"** and gains `id="download-data"` plus a focusable heading (`tabIndex=-1`, `data-export-target`), so the jump lands keyboard and screen-reader users on the heading sighted users scrolled to; `scrollIntoView` honours `prefers-reduced-motion`. `NarrativePanel` gains `mdBusy`/`mdError` beside the PDF states with busy labels inline in the menu items. Commit `586da1a`. |
| 78 | BigQuery GQL export — the plan-§7.6 "cloud-scale analysis with GQL queries" export ships: **`GET /export?format=gql`** (and `POST /export-network`) packages the assembled BODS bundle as a **BigQuery property graph**, generated with [bods-gql](https://github.com/StephenAbbott/bods-gql) — now a git dependency **pinned to `362973e`**, made embeddable by an upstream change landed first (bods-gql PR #1: the Google Cloud stack — google-cloud-bigquery, bigquery-storage, pyarrow, ~100MB — moved to a `bods-gql[bigquery]` extra, so OpenCheck's core install adds only `click`). `bods-xml` pinned to a commit in the same change so neither git dependency can drift silently. The `-bigquery.zip`: `entity_nodes.csv` / `person_nodes.csv` / `ownership_edges.csv` (empty tables skipped — header-only CSVs choke `bq load --autodetect`), `create_property_graph.sql`, the library's **14 GQL (ISO/IEC 39075) queries** under `queries/` (UBO detection, corporate groups, circular ownership — the bods-neo4j Cypher suite translated), a README with the `bq load` steps, and `LICENSES.md`. DDL and queries target a `YOUR_PROJECT.YOUR_DATASET` placeholder with the swap one-liner documented. `bods/gql.py` is a thin wrapper (`map_to_gql` / `gql_counts` / `build_gql_files`; query filenames match the bods-gql CLI's `--query-type` names); the `/export` format regex now derives from `_EXPORT_FORMATS` so the two can't drift, and both ZIP-bundle manifests gain `gql_*` counts. Frontend: Download data panel option + explainer, FullCheck network export option. `okf/api/export.md` formats table brought current (senzing/ftm were missing too). `tests/test_gql_export.py` pins zip membership, CSV headers, placeholder discipline, empty-table skipping, manifest counts, the network route, and that the `ExportNetworkRequest` Literal equals `_EXPORT_FORMATS`+cypher. Commit `4953cf4`. |
| 79 | Google AML AI export — the **last** plan-§7.6 export ships, completing the roadmap: **`GET /export?format=amlai`** (and `POST /export-network`) packages the assembled BODS bundle as [Google AML AI](https://docs.cloud.google.com/financial-services/anti-money-laundering/docs/reference/schemas/aml-input-data-model) input tables via [bods-aml-ai](https://github.com/StephenAbbott/bods-aml-ai) — a git dependency **pinned to `11d14ca`**, the upstream change landed first that (a) added the in-memory API this endpoint uses (`process_statements` / `transform` / `transform_statements` — no files read or written) and (b) **fixed a latent upstream bug**: the library's output writer had been swallowed by an unanchored `output/` gitignore rule, so `bods_aml_ai.pipeline` failed to import on every fresh clone. AML AI's model is account/transaction-centric with **no party-to-party relationship table**, so ownership is encoded through its extensibility mechanisms: persons → `CONSUMER` and entities → `COMPANY` Party rows; each interest decomposed into numeric `party_supplementary_data` signals (`bo_ownership_pct_<subject>`, `bo_is_beneficial_owner`, `bo_is_direct`, per-interest-type flags, per-subject aggregates); and synthetic "ownership accounts" (`bods-ownership-<subject>`) in `account_party_link` — owned entity as `PRIMARY_HOLDER`, each owner as `SUPPLEMENTARY_HOLDER` — so AML AI's graph-based scoring still sees the connections. The `-aml-ai.zip`: the three NDJSON tables (empty tables skipped), a README explaining the encoding + `bq load` steps, `LICENSES.md`; both ZIP-bundle manifests gain `aml_ai_*` counts. Frontend: Download data panel option + explainer, FullCheck network export option. `tests/test_aml_ai_export.py` pins the ownership-signal encoding, zip membership, NDJSON validity, empty-table skipping, manifest counts and both routes. Commit `30715dc`. |
| 80 | Hotfix: `format=gql` 500 on real data — the deployed BigQuery GQL export failed its first live request (`dict contains fields not in fieldnames: 'jurisdiction_name', 'jurisdiction_code'`): bods-gql's `to_dict()` drops `None` values, so mapped row key sets **vary with the data** — BP's first mapped entity carried no jurisdiction, a later one did, and `csv.DictWriter` (fieldnames from row 0, the same approach as the bods-gql CLI, which shares the latent bug) raised on the unknown keys. The Phase-78 fixtures all had identical shapes, so 2396 green tests never caught it — the recorded spike lesson verbatim: *fixtures are not real data*. Fix: `_rows_to_csv` now takes the **full dataclass field list** (`dataclasses.fields(EntityNode/PersonNode/OwnershipEdge)`) as a fixed column schema with `restval=""` — every export gets the same stable column set (matching the upstream BigQuery loader schemas) regardless of which fields the data populates. Regression test pins the varying-shape case; verified against BP's live 925KB bundle (248 entity rows + 323 edge rows, uniform columns; `person_nodes.csv` correctly skipped — the BP graph is corporate-only). Same fix worth porting to the bods-gql CLI's `to-csv`. Commit `506226f`. |
| 81 | RDF export — BODS as linked open data: **`GET /export?format=rdf`** (and `POST /export-network`) renders the assembled bundle as TriG following [Open Ownership's documented conversion pattern](https://vocab.openownership.org/pages/4_convertingdata.html) (one named graph per statement), using **only published BODS-vocabulary terms for the data** — validated against [`bods-vocabulary-0.4.0.ttl`](https://vocab.openownership.org/terms/bods-vocabulary-0.4.0.ttl): 66 vocabulary terms used, 0 unknown, with 5 private `oc:` terms confined to the analytical layer. Two capabilities no other OpenCheck format has: **(1) licensing travels with the data** — every statement carries `bods:license` with its source's canonical licence URI, resolved from the licensing registry exactly as the export ZIP's `LICENSES.md` is; **(2) the analytical layer ships as a detachable overlay** — risk signals (`codes:Commenting`), `POSSIBLY_SAME_AS` links (`codes:Linking`) and degraded screens become `bods:Annotation` nodes (`bods:motivation`, `bods:statementPointerTarget`, `bods:createdBy`) in a separate named analysis graph (`https://opencheck.world/analysis/{lei}/{date}`) targeting the Phase-45 stable statement ids — derived analysis expressed over BODS statements without forking the standard. `legalFormLabel` maps to standard terms (`bods:entityType` `codes:LegalEntity` + `bods:entityTypeDetails`), so the ELF work needs zero invented predicates. Implementation: `bods/rdf.py` (rdflib, pure function like `senzing.py`/`ftm.py`/`neo4j.py`; handles OO-bulk field variants like `incorporatedInJurisdiction`, reporting-exception unspecified parties via `bods:Unspecified`, and cleans/drops malformed identifier URIs found in real bulk data). Bulk side: `scripts/export_rdf_bulk.py` streams JSONL→NQuads chunk-wise (memory-flat; `--fallback-license` for source-less statements) — the [Estonia dataset release](https://github.com/StephenAbbott/opencheck/releases/tag/dataset-estonia-2026-07-04) converts in ~2 min (209,529 statements → 6,638,471 quads; register layer CC-BY-4.0 from source blocks, GLEIF layer CC0 via fallback) and is **published on that release as `estonia-2026-07-04.nq.gz`**. `scripts/rdf_demo_queries.py` is the RDF twin of the release's `demo_queries.cypher`: the same demo questions in DuckDB SQL over the quads via the community [duck_rdf](https://duckdb.org/community_extensions/extensions/rdf) extension, with the release's expected numbers **asserted** — all matched (incl. the 5-company Kaamos shareholding chain verbatim) — plus the two RDF-only queries (statements-per-licence; per-edge source + licence + retrieval time from each edge's named graph). Bears on the parked Phase-8 graph-DB question: the 6.6M-quad corpus loads in ~10 s, a 3-hop traversal from **all 23,578 LEI companies simultaneously** takes ~2.5 s, single-anchor multi-hop traversals are milliseconds — far inside the >2 s revisit trigger, with no graph database running. Gotchas recorded: duck_rdf cannot read gzipped NQuads (refuses the `.nq.gz` double extension; a `file_type` hint parses the compressed bytes raw) — `rdf_demo_queries.py` transparently decompresses `.gz` to a temp file; duck_rdf preserves CURIEs when reading TriG but yields full IRIs from NQuads. Seven domain/range glitches found in the published 0.4.0 ttl (e.g. `bods:source` scoped to `PoliticalExposure` only; the conversion docs' `NewRecordStatement` absent from the ttl) [reported upstream on openownership/bodsld](https://github.com/openownership/bodsld/issues). Frontend: RDF option on the Export panel (with explainer), the FullCheck network exporter, and the API page — whose hand-maintained `/export` format list had drifted (gql and amlai were missing too) and now documents all nine formats. `rdflib>=7.0` added; 17 new backend tests; full suite 2,442 passed. PRs #63 / #64 / #65. |
| 82 | BackgroundCheck (spike) — screening the **people** connected to an entity, returning to plan-§3's unbuilt "person lookups". A third check mode beside QuickCheck/FullCheck lists the connected people extracted client-side from the assembled BODS bundle (`lib/backgroundCheck.ts` walks person + relationship statements — source-agnostic, so Estonian officers/beneficial owners/shareholders surface exactly like UK officers and PSCs), merging the same person across sources by normalised name + birth year while keeping every statementId for traceability. Per-person on-demand **`GET /person-check?name=&birth_year=`** (`routers/person_check.py`) fans the name out across every person-capable adapter (CH officers, OpenSanctions, EveryPolitician, Wikidata, OpenAleph), scores each hit with the cross-check module's `_name_score`/`_birth_year_compatible` helpers at the **same 0.88 threshold** ("strong match" is one concept product-wide), and derives risk signals via `assess_hits` **from strong matches only** — every person-level signal carries an `evidence.match` block (query name, similarity, birth-year corroboration), a birth-year mismatch blocks strong status even at 100% name similarity, and the response lists every checked source with attribution/licence/hit-count/error so a failed source renders as "not screened" (never silently dropped) and "no signals" is always qualified. UI: violet person cards with roles + the company each is held in, Hide/Show per completed check, capped sequential "Check all" with live progress, checked-at timestamps, below-threshold matches behind a disclosure; entity-scoped panels (AI summary, risk signals, cross-source identifiers, possibly-same) hide while the mode is active. Checks run on demand only — bounded OpenSanctions free-tier load, and no unsolicited claims about people. Full brainstorm, design critique and the Phases A–F rollout plan (EveryPolitician promotion, shareable person report) live on the Notion ticket; scope + known gaps in `docs/background-check.md`. **Phase C — identity enrichment**: `GET /person-appointments?officer_id=` returns every Companies House appointment under one officer id (the register's own same-person assertion — stronger than any name match) with the BODS personStatement carrying the `GB-COH-OFFICER` identifier, surfaced as "View appointments across companies" on strong CH matches; `/person-check` reconciles **strong matches only** into `cross_source_links` (shared Q-ID / OpenSanctions id — weak matches excluded so bridging never manufactures a same-person claim), rendered as a "Same person across sources" panel; Wikidata person searches post-filter to instance-of-human (P31→Q5, one batched SPARQL VALUES query, failing open on errors) — killing the exact-name noise the Phase-B live smoke surfaced ("Tony Blair" the painting / the Chumbawamba single) for `/search?kind=person` too; and same-name/missing-birth-year pairs in the people list are flagged "possibly the same person" for human review, never auto-merged. **Phase D — EveryPolitician first-class**: `GET /person-positions?entity_id=` returns the full positions-held history behind an EveryPolitician / OpenSanctions PEP record (post + country + start/end dates parsed from FtM `positionOccupancies`, most recent first, open-ended positions marked *current*), surfaced as "View positions held" on strong EP matches — a dated timeline with the Wikidata Q-ID key, Poliloom maintenance note, canonical OpenSanctions record link, and the plan-§4.5 coverage caveat, which now also rides on **every** person-check response rather than only non-hits. **Phase E — person report as a first-class artefact**: the person report is URL-addressable (`?person=<name>&person_birth_year=<yyyy>`, pushState/popstate beside `?lei=`; the person param takes render precedence and leaves the entity state untouched, so back/forward between entity and person is instant) with a copy-link button; connected-person cards gain "Open person report page" and "Download BODS (person subgraph)" — the person's statements + relationships + subject companies as a valid flat BODS v0.4 array with source blocks kept; `opencheck_person_check` joins the MCP server (evidence-shaped: signals from strong matches only, raw hit payloads dropped, per-source outcomes and caveats included; descriptor now lists 6 tools); and a **TENTATIVE** homepage "Person name" search tab (name + optional birth year → person report) ships deliberately isolated in one commit for a clean revert if entity-anchored person search proves the better sole entry point. Commits `9ff3810`, `0cf0db7`, `930fe12`, `9442be4`, `66cffce`, `ea321ca`, `69ab0ca`, `3ff518d`, `d65d678`. |
| 83 | EITI State-Owned Enterprises Database — a new LEI-keyed source (`eiti_soe`) surfacing state-owned enterprises reported through the EITI, distinct from the existing `eiti` payments adapter. SOEs carry no native LEI, so each is resolved to an LEI at index-build time via GLEIF (`scripts/build_eiti_soe_index.py`) into a committed index; `map_eiti_soe` emits a `stateBody` government + `controlByLegalFramework` relationship, which raises the existing `STATE_CONTROLLED` signal with no risk-engine or frontend changes. Category `cdd`; licence clean (EITI direct, not the CC-BY-NC OpenSanctions mirror). Commit `7f54617`. |
| 84 | Identifier validation — rigour Phase A. The [rigour adoption plan](https://github.com/opensanctions/rigour)'s first phase ships: a shared `opencheck/identifiers.py` replaces the LEI shape regex that had been copy-pasted across **eleven** call sites in two incompatible shapes, and OpenCheck validates **ISO 17442 check digits** for the first time — previously a mistyped LEI passed every gate and burned a GLEIF round-trip before dying as a generic 404. At the API boundaries (`/lookup`, `/lookup-stream`, `/history`, `/subsidiaries`, `/securities`, `/share`) an LEI with bad check digits now fails fast with an explanation; in the data paths a checksum-invalid value is no longer *classified* as an LEI — the BODS FtM/Senzing mappers stop mislabelling LEI-shaped registration numbers as `leiCode`, the reconciler stops promoting them to strong cross-source merge keys (Wikidata QIDs there are shape-validated too), Wikidata's hand-entered P1278 claims and OpenTender's `ETALON_ID` shape-sniffing get the same guard. Validation prefers `rigour.ids` — the same implementation OpenSanctions runs, now an explicit `ftm`-extra dependency (`rigour>=0.14,<3`) rather than a transitive accident of bods-ftm → followthemoney — with a parity-tested pure-Python mod-97 fallback so ICU-less dev installs behave identically (the `matching.py` belt-and-braces pattern). Beyond LEIs: securities rows carry an ISIN **Luhn** `checksum_valid` data-quality flag (surfaced, never dropped); `/resolve-national-id` returns an advisory `checksum_warning` when a Finnish Y-tunnus, Swedish organisationsnummer or Brazilian CNPJ fails its check digit (the query still runs — the registry is the authority); and the PRH, Bolagsverket and CNPJ adapters skip the upstream round-trip entirely for numbers whose check digits mean they cannot exist in the register (python-stdnum, already in the ftm tree). Enforcement is governed by `OPENCHECK_IDENTIFIER_CHECKSUMS_ENFORCED` (default on; the test suite disables it exactly like the rate limiter, because long-standing fixtures use deliberately fake shape-valid LEIs) — `dispositions` deliberately keeps shape-only validation so stored disposition keys stay readable. 25 new tests incl. rigour-parity pins; full suite 2,555 passed. Commit `52bc981`. |
| 85 | rigour Phases B–E — name normalisation, org-type merge keys, shared scoring, languages. Completes the [rigour adoption plan](https://github.com/opensanctions/rigour)'s remaining phases in one arc. **Phase B**: a shared `opencheck/names.py` replaces the **five** divergent hand-rolled name normalisers (cross_check + its verbatim icij_check duplicate — each with its own `_NON_DECOMPOSABLE_FOLDS` copy, both deleted — reconcile with no folds at all, openaleph, nz_associations). The pipeline is **deterministic across environments** (prod builds pyicu, dev doesn't, and normality's ICU-less fallback renders `Ø` as `O/` — so every transform is a plain table or stdlib Unicode op): casefold → extended non-decomposable folds → NFKD strip → bounded Cyrillic/Greek→Latin transliteration (`Газпром`→`gazprom`, `ЛУКОЙЛ`→`lukoil`) → punctuation/squash; CJK deliberately passes through. Native vs transliterated forms of the same name now score >0.85 instead of ~0, and corpus parity is pinned: every Latin-script demo-corpus name normalises exactly as before, so the 0.88 threshold doesn't move. Also fixes the Greek-homoglyph identifier hole — `canonical_identifier` folds lookalike capitals first, so Cyprus `ΗΕ 489243` and GLEIF's `HE 489243` finally share a cross-source key (previously: empty string in the fallback path). **Phase C**: `possibly_same_entities` candidate keys are org-type aware via rigour's curated data (`replace_org_types_compare`, generic classes) — 'Tesco Stores Limited' ≡ 'TESCO STORES LTD', ооо ≡ GmbH ≡ Ltd — plus a despaced plain-form secondary key that collides Danish `A/S`↔`AS` (deliberately NOT the org-typed form: rigour aliases bare `AS` but not `A/S`); sec_edgar keeps its trailing-token strip (rigour 0.14's data misses bare `COMPANY`/`CO`) but gains the shared fold base. **Phase D**: `names.name_similarity()` is THE scorer behind RELATED_PEP/RELATED_SANCTIONED and BackgroundCheck — the historical difflib ratio (floor: no pair scores lower than before, provably recall-only) plus token-sort order invariance ('Doe, John' ↔ 'John Doe'; NZ's 'LastName First') and rigour's edit-budgeted Levenshtein; `is_matchable_name` no longer blocks CJK/kana/Hangul names, whose scripts have no spaces for the single-token guard to find. `scripts/eval_name_matching.py` pins the before/after: zero demo-corpus pairs cross 0.88 in either direction. **Phase E**: Cyrillic/Greek entity names gain a deterministic Latin `alternateNames` entry and person statements a typed `transliteration` name (v0.4 nameType codelist, verified against libcovebods' schema); the Wikidata fetch captures labels in ten languages instead of @en only, emitted as alternate names; `normalise_language_code()` gives ISO 639 alpha-3 normalisation (rigour, pycountry fallback). rigour 2.x's richer names pipeline (analyze_names, compare_parts, ICU transliteration) stays out of reach while bods-ftm's followthemoney pins rigour to 0.x — the switch points are documented in names.py. 40 new tests across the four phases; full suite 2,586 passed. Commits `85a288d`, `07a8645`, `8b5abe7`, `956128c`. |
| 86 | followthemoney 4.x / rigour 2.x — the dependency ceiling behind the rigour adoption lifts. bods-ftm (upstream [PR #1](https://github.com/StephenAbbott/bods-ftm/pull/1), pinned `9347d80f`) now runs followthemoney 4.x, removing the `rigour <1.0` cap that had held OpenCheck's Phases 84–85 on the 0.x line: the `ftm` extra pin moves `rigour>=0.14,<3` → `rigour>=2.2,<3` and resolves **rigour 2.3.1** — the Rust-cored line with ICU transliteration, curated org-type data (466 legal-form specs) and script detection compiled in, prebuilt wheels for linux/macOS (incl. arm64)/windows cp310–cp314 — alongside followthemoney 4.10.1 and python-stdnum 2.2. One planning assumption inverted along the way: FtM 4.x does not drop the pyicu build — it makes it **mandatory** (every FtM ≥4.2 requires normality 3.x, which hard-depends on sdist-only pyicu), so the Docker image and CI keep g++/libicu-dev/pkg-config, macOS dev installs of the extra need `brew install icu4c pkg-config`, and the parity-tested pure-Python fallbacks in identifiers.py/names.py stay — now serving only installs *without* the extra. Treated as a matching-behaviour change per the plan: full suite green with rigour 2.3.1 active in the identifier/name paths, `opencheck/ftm.py` parity holds (bods-ftm needed zero converter API changes on FtM 4.x), and the `scripts/eval_name_matching.py` gate shows zero demo-corpus pairs crossing the 0.88 threshold in either direction. This makes the names.py switch points reachable — `maybe_ascii` transliteration, `text_scripts` script detection, sec_edgar's org-type strip (rigour 2.x has the bare `COMPANY`/`CO` specs 0.14 lacked), native BCP-47 parsing, and the `analyze_names`/`compare_parts` alignment scorer — cashing them in is the next arc. Commit `5ce78c2`. |
| 87 | rigour 2.x switch points in `names.py` — cashed in. The four switch points Phase 85 documented and Phase 86 unlocked are resolved, one per commit with a `scripts/eval_name_matching.py` run before/after each (all four: zero demo-corpus pairs crossing 0.88 in either direction) and both-path tests pinning `ftm`-extra and base-install behaviour. **Transliteration — cashed as a hybrid, a deliberate product decision**: rigour 2.3.1's Rust-cored `maybe_ascii` turns out to be ISO-9-flavoured, systematically diverging from the BGN/PCGN-style Latin forms OpenSanctions publishes (`Чехов`→`Cehov` vs `chekhov`, `ЛУКОЙЛ`→`LUKOJL` — scoring 0.83 against published `Lukoil`, below the 0.88 threshold; `Ельцин`→`El'cin`, whose apostrophe splits the token), so a full replacement would have broken exactly the native↔published-Latin matches Phase B built the tables for. Instead the curated Cyrillic/Greek tables stay authoritative and `maybe_ascii` runs over **Armenian/Georgian runs only** — the scripts the tables never covered (`Ամերիաբանկ`→`ameriabank`, matching the bank's published Latin name exactly) — in both `fold_ascii` and `transliterate_display` (BODS alternates); Hangul stays excluded (RR romanisation `김정은`→`gimjeong-eun` mismatches published `Kim Jong-un` and would re-break the dense-script guard); a canary test pins rigour's divergence so a future scheme change reopens the question. **Dense-script detection — cashed**: `has_dense_script` delegates to `rigour.text.scripts.is_dense_script` (Unicode script properties), and the switch exposed a latent bug both paths now fix — `normalise_name` NFKD-decomposes Hangul syllables to conjoining Jamo, which the old syllables-only ranges didn't recognise, so every *normalised* Korean name silently failed `is_matchable_name`'s dense-script guard (Phase D's test only probed precomposed strings); the extra-less fallback ranges gain Jamo, halfwidth katakana and CJK Extensions B–G, with an agreement test pinning both paths to identical answers. **sec_edgar strip + `A/S` despace twist — re-verified and kept**: contrary to the Phase 86 planning note, rigour 2.3.1's org-type data still leaves bare `COMPANY`/`CO` untouched (deleting the strip would regress `THE WALT DISNEY COMPANY` ↔ `Walt Disney Co`) and still doesn't alias `A/S`, so both stay, now with canary tests that fail the moment a future rigour closes either gap — the failing assertion is the signal to simplify. **BCP-47 retry — cashed (deleted)**: rigour 2.x's `iso_639_alpha3` parses `zh-Hans`/`zh_Hant`/`sr-Latn-RS` natively, so the 0.x-era primary-subtag retry is gone; unparseable tags still fall through to the pycountry fallback, which does its own subtag split for base installs. Upstream, in the same arc: bods-ftm's shipped `examples/sample_bods.json` predated the canonical 0.4 envelope and converted to **zero** FTM entities via the README's own walkthrough — rebuilt as canonical 0.4, `sample_ftm.jsonl` regenerated via the CLI (7 entities), and smoke tests pin the pair to the converter so it can't rot again (bods-ftm commit `ff8a5dd`). The `analyze_names`/`compare_parts` alignment scorer stays deliberately out of scope (own eval design, per the ticket). 15 new backend tests; full suite 2,618. Commits `0a677c6`, `b7d8a04`, `a1dd695`, `9b4a829`. |
| 88 | SEO entity pages — a server-rendered `opencheck.world` URL for every LEI, built from GLEIF bulk data alone. Phases A–C of the SEO plan: `scripts/build_entity_pages_db.py` streams the GLEIF Golden Copy LEI2+RR files (~3.4M records; full rebuild or `--delta LastMonth` for the monthly refresh, via GLEIF's free publish API) into `entity_pages.sqlite`, and `/entity/{LEI}-{slug}` renders each entity as real HTML with the ticket's exact furniture — title `NAME - OpenCheck`, the fixed meta description, canonical URL, Open Graph and JSON-LD `Organization`/`leiCode` — plus GLEIF reference data (status badge, address, ELF legal form, parents/children from RR, successor links), stale-slug 301s, ETag/304 and day-long caching. Sitemap index + 50k-URL shards, `/browse/{country}` hub pages for internal-link discovery, and a backend `robots.txt` that disallows the live-lookup API: the design rule throughout is **GLEIF-local only** — a crawler can fetch all 3.4M pages without a single third-party adapter call; live checks fire only from the page's CTA into the SPA's `?lei=` flow. Deployment: Render static rewrites can't proxy cross-service (verified against Render docs), so the cutover is single-service mode — `OPENCHECK_FRONTEND_DIST` serves the built SPA from FastAPI (dist baked into the Docker image, inert until set), with Accept-header negotiation keeping `GET /sources` JSON for API clients while browsers get the SPA view. Routes are deliberately un-rate-limited (Googlebot 429s collapse crawl rate; every handler is a sub-ms local SQLite read) and exempted in the rate-limit invariant test; the DB downloads at boot from `OPENCHECK_ENTITY_PAGES_DB_URL` on ephemeral-disk hosts, with 503+`Retry-After` until it lands. 18 new backend tests; full suite 2,625. Commit `d7efd38`. |
| 89 | Privacy-respecting analytics — GoatCounter, wired so subjects can never leak. The 'Privacy-respecting analytics' ticket ships: cookie-less GoatCounter on both surfaces — the SPA (`frontend/src/lib/analytics.ts`, `no_onload` + patched `pushState`/`popstate`, live-host guard) and the server-rendered `/entity`+`/browse` pages (snippet emitted by `entity_pages.py`, `OPENCHECK_GOATCOUNTER_ENDPOINT` to override or disable). The privacy contract from the ticket is enforced at a single choke-point, `canonicalPath`: `?lei=` → `/lookup`, `?person=` → `/person-check`, `/entity/{LEI}-{slug}` → `/entity`, `/browse/{CC}` → `/browse`, anything unrecognised → `/` — no LEI, person name or query string is ever recorded, pinned by vitest tests that regex-scan outputs for LEI shapes. Feature usage is counted as anonymous events (`lookup_run`, `fullcheck_run`, `backgroundcheck_run`, `pdf_export`, `share_link`, `graph_expand`, `entity_page_cta`), never as subjects — per-entity search interest comes from Google Search Console instead. Disabling the endpoint removes every GoatCounter trace including the CTA event and the footer privacy note; 404/503 pages carry no analytics at all. Privacy notes added to the entity-page footer and a new 'Privacy & analytics' card on the About page. 4 new backend tests (22 in test_entity_pages.py) + 5 vitest (134 total). Commit `42d8c0f`. |
| 90 | Monthly GLEIF refresh automation — Phase E of the SEO entity-pages plan, closing the loop opened at the Phase 88 go-live. `.github/workflows/refresh-entity-pages-db.yml` (house style of the securities-index refresh) rebuilds `entity_pages.sqlite` **in full** from the latest Golden Copy publish on the 1st of each month at 06:30 UTC, sanity-checks the result (≥3.3M entities and a real publish date — refusing to publish a partial build), gzips and replaces the `entity-pages-latest` release asset the backend boots from, writes the count into the release notes and the job summary, and optionally triggers a Render redeploy via the shared `RENDER_DEPLOY_HOOK_URL` secret. A deliberate divergence from the original plan's 31-day-delta design: the runner is stateless, so a delta run would first download the previous DB (~the same bytes as the full CSV) and would inherit month-boundary gap risk — a full rebuild costs about the same and self-heals any drift (`--delta LastMonth` remains for local use). Runs on plain `python3` + httpx: the builder's import surface is stdlib-only by design, verified by running the exact CI command against real GLEIF delta files. Commit `94fd8f4`. |
| 91 | IndexNow — SEO Phase D's push channel, riding the monthly refresh. `scripts/submit_indexnow.py` maps the GLEIF LastMonth delta (every LEI issued or revised in the preceding 31 days, typically ~100–300k) to canonical `/entity/{LEI}-{slug}` URLs — the same `slugify_name` as the DB builder, pinned by a test so submitted URLs always equal the pages' canonical URLs — deduplicates, and POSTs the protocol's 10,000-URL batches to `api.indexnow.org` (Bing, Seznam, Naver, Yandex and friends; Google ignores IndexNow, its discovery stays sitemap/`lastmod` via Search Console). Ownership is proved by a new backend route `GET /indexnow/{key}.txt` echoing `OPENCHECK_INDEXNOW_KEY` (404 when unset or mismatched; rate-limit exempt like the rest of the crawl infrastructure), and the monthly refresh workflow gains a final optional step driven by the matching `INDEXNOW_KEY` Actions secret — no key, no-op. Dry-run verified against a real GLEIF delta (1,121 URLs, slugs byte-identical to the live pages). 3 new tests; full suite 2,632. Commit `bfe8407`. |
| 92 | TED — Tenders Electronic Daily: live "has this entity won EU public contracts?". New identifier-keyed adapter `ted_eu` (EITI dispatch pattern — applies to any LEI holder in a TED-relevant jurisdiction, no RA deriver): one anonymous POST to the TED Search API v3 querying `organisation-identifier-tenderer` (eForms BT-501) with the LEI + GLEIF `registeredAs` (verbatim **and** separator-stripped — TED stores whatever the buyer entered, `HRB 6684` space included) + derived national numbers, then the newest ≤10 notices winner-confirmed from the eForms notice XML (`LotResult → LotTender → TenderingParty → Tenderer → CompanyID`) and labelled **won** / **tendered** / unconfirmed with per-lot awarded values, award dates and contract references. Investigation facts baked in (all live-verified, "EU TED eForms" Notion ticket): the LEI fill rate in BT-501 is currently **zero** (0 in 5,031 sampled identifiers, July 2026) so recall comes from the national registration numbers OpenCheck already derives — the LEI rides in every query at zero cost, so the lookup self-upgrades as eForms LEI adoption grows; identifier fields are exact-match only; `scope` must be `ALL`; coverage is the eForms era (≈2024+) only, disclosed on the card alongside the subsidiary caveat. Licence-clean where OpenTender was not: Decision 2011/833/EU permits commercial reuse. Frontend award list in the source card; hit identifiers follow the corroboration rule (national ids never echoed back; `lei` asserted only when a notice actually carried it). BODS: entity statement only (EITI precedent — awards are activity, not ownership). 23 new tests + a TED live-smoke pair, which caught a real 406 (notice XML needs an explicit XML `Accept` header). Full suite 2,655. Commit `4094020`. |
| 93 | India — MCA Company Master Data: the world's largest active-LEI country joins the register lookup. New identifier-keyed adapter `mca_india` over the Ministry of Corporate Affairs **Company Master Data** on the Open Government Data Platform (data.gov.in) — India's national company register extract (~3.67M companies; GODL licence, so commercial-clean), resource `4dbe5667` live-verified 2026-08-08 with `updated_date` 2026-07-22. GLEIF bridge: `registeredAt.id == RA000394` → 21-character CIN (`in_cin` deriver) → one exact `filters[CIN]` query returning name, status, class/category/sub-category, authorised & paid-up capital, registration date, RoC, registered office address, listing status and NIC classification, mapped to a single BODS entity statement (`IN-MCA` identifier scheme; conservative terminal-status → `dissolutionDate: unknown` — in-progress and dormant states deliberately excluded). Investigation facts baked in: every OGD field is Elasticsearch `keyword` type — **exact match only**, names stored UPPERCASE — so `search()` is exact-name-only and the LEI→CIN flow is primary; the platform's HTTP 429 rate-limit answer is handled as a stub, not an error. Ships in **stub mode**: generating a personal `DATA_GOV_IN_API_KEY` currently requires a JanParichay/MeriPehchaan login whose SMS OTP is never delivered to non-Indian (+44/+32) numbers — escalated in parallel to NIC (`support-parichay`) and the OGD/NDSAP team, with the login page's Google/GitHub OAuth route and an India-based colleague as fallbacks ('India / MCA Company Master Data' Notion ticket). Fixtures captured live via the platform's documented shared sample key (~1-in-5 requests beat the 429), so tests run against real payload shapes and the adapter flips live by setting one env var. 22 new tests; full suite green. Commit `1a633db`. |
| 94 | Curated examples refresh — the homepage picker swaps three UK bulk-BODS subjects (Bank Saderat PLC, Hornsea 1 Limited, Newcastle United FC) for three **live-lookup** examples: **Eesti Energia AS** (`5493005044RTLQ5RZU70`, Estonian energy company), **Ørsted A/S** (`W9NG6WMZIYEU8VEDOG48`, Danish offshore energy company — the very `A/S` name shape that motivated the quote-safe screening work, now a first-class example) and **Eli Lilly and Company** (`FRDRIPF3EKNDJ2CQJL29`, American pharmaceutical giant); BP, Rosneft and Taqa Bratani stay as they are. Card risk signals captured from production lookups 2026-08-09 (Eesti Energia: `STATE_CONTROLLED` / `RELATED_PEP` / `COMPLEX_CORPORATE_STRUCTURE`; Ørsted: `COMPLEX_CORPORATE_STRUCTURE` / `OFFSHORE_LEAKS` / `COMPLEX_OWNERSHIP_LAYERS`; Eli Lilly: `RELATED_PEP` / `OFFSHORE_LEAKS` / `NON_EU_JURISDICTION`). A new `bulkBods` flag on `EXAMPLE_LEIS` scopes the blue "Curated example — pre-extracted data" banner to the bulk-BODS trio only — the new examples run as ordinary live lookups. All six examples keep instant, auto-shown pre-baked AI summaries: `CURATED_LEIS` synced (guarded by `test_curated_leis_sync.py`), `build_curated_narratives.py` gains an argv-subset mode so one example can be (re)generated without churning the rest — three new summary JSONs generated with claude-sonnet-4-6, three stale ones removed. Every example keeps its homepage Neo4j-zip link: the three new zips are **live-export snapshots** (`GET /export?lei=&format=jsonl` → `bods-neo4j to-csv`) and — because live exports combine sources beyond OGL/CC0 (OpenSanctions CC-BY-NC-4.0, OpenCorporates OC-Terms) — each carries its own `LICENCES.md` (per-source matrix + combined-use verdict) built with the export router's `_build_licenses_md`; the live-export recipe is documented in `export_neo4j.py` and scoped out of the bulk demo licence notes in `data/demo/LICENCES.md`. The bulk demo zips for the removed examples remain published. Commit `b862556`. |
| 95 | Nigeria CAC — the first African beneficial ownership register in OpenCheck. New offline source `cac_nigeria` over the Corporate Affairs Commission Persons with Significant Control register ([bor.cac.gov.ng](https://bor.cac.gov.ng)) — Africa's first public BO register (CAC; Open Ownership partner country). The CAC register is fully public but its **official API is restricted to Nigerian government agencies**, so rather than scrape the site's private JSON API at request time, a **curated example set of 10 LEI-anchored Nigerian companies** is harvested once and committed as an LEI-keyed index (`data/cac_nigeria_psc.json`, built by `scripts/build_cac_nigeria_index.py` from the committed harvest `data/cac_nigeria_raw.json`); the adapter answers `fetch_by_lei` as an offline dict lookup and `map_cac_nigeria` maps each record to BODS v0.4 — no network on the hot path. **A live adapter is deferred pending engagement with the CAC and its technical partner Oasis Management** (tie-in: the CAC's Nov 2026 Global Conference on Beneficial Ownership and Asset Recovery); the vendored source is the scaffold it slots into (same `source_id`, mapper, schema). GLEIF RA code **`RA000469`** (verified live 2026-08-12) — added to the RA-codes table. LEI-keyed dispatch alongside `bods_gleif`/`eiti_soe` (no RA deriver); per the corroboration rule it asserts only the CAC-published RC number (`ng_cac_rc`), never the LEI, which OpenCheck derives via GLEIF. The five statutory CAMA PSC conditions map to `shareholding` / `votingRights` / `appointmentOfBoard` / `otherInfluenceOrControl` (×2, company + trust/firm), with `beneficialOwnershipOrControl` asserted only for natural persons — corporate/intermediate owners (e.g. Dangote Industries, controlling both Dangote Cement and Dangote Sugar through one shared statement) are flagged false. Owner names arrive transposed / duplicated / typo'd in the source and are normalised at build time (a live adapter would need heuristics); blank owner rows are dropped and dates of birth omitted for privacy. Deliberately **not** added to the homepage curated examples (`EXAMPLE_LEIS`) — it surfaces only as a source card in the live lookup for the 10 LEIs. 12 new tests (incl. a regression that maps the real committed index with zero validation issues); full suite green. Commit `ba5f86b`; PR [#104](https://github.com/StephenAbbott/opencheck/pull/104). |
| 96 | OpenAleph text-based percolation — the upstream feature OpenCheck asked for, wired into the lookup. OpenCheck's feature request [openaleph/openaleph#105](https://github.com/openaleph/openaleph/issues/105) (filed 2026-07-02, PR offered and accepted — upstream PRs [#107](https://github.com/openaleph/openaleph/pull/107)/[#120](https://github.com/openaleph/openaleph/pull/120)) shipped in OpenAleph [5.3.1](https://github.com/openaleph/openaleph/releases/tag/5.3.1) as `POST /api/2/beta/percolate`: POST arbitrary text, get back the stored entities whose name-percolator queries (`match_phrase` over the FtM names group, slop 2) fire on it — each hit carrying `percolator_match` (canonical name vs alias tier), `surface_forms` (the exact input phrase that fired) and `score`. Phase 1 of the build-out: `percolate_text()` on the adapter (key-gated like `/match` — the flagship edge 405s anonymous POSTs, verified live 2026-08-13; `None` = screen could not run vs `[]` = ran-and-clean, the distinction the coming graph-screening phase hangs degradation records on; cache key covers text **and** every filter param) and `fetch_by_name_percolate()`, a new subject-name strategy slotted between the FtM `/match` step and the Lucene `q=` fallback. The legal name travels as raw JSON body text — never through Aleph's query_string parser — so the reserved-syntax bug class that took three rounds to tame (quotes — PR [#67](https://github.com/StephenAbbott/opencheck/pull/67) —, `A/S`, LVMH's dangling `+`) structurally cannot occur on this path; the `_bears_name` gate stays (percolation is necessary, not sufficient: an entity named 'Shell' fires on 'Shell Midstream Partners', slop 2 admits inserted tokens), and the `q=` fallback survives for keyless deployments, which behave exactly as before. Live findings baked into the docstrings: always pass a selective filter — unfiltered percolation over famous company names drowns in near-duplicate registry records (Shell + BP → 43 hits, top 15 all Companies House-PSC/CorpWatch copies) while `filter:schema=Person` is high-precision (Igor Sechin → 8 watchlist records across FCDO/OFAC/SAM/disqualified-directors; non-notable executives → nothing) — the shape of the Phase 2 related-party graph screening to come. 17 new tests; full suite 2,673. Commit `54e23ea`. |
| 97 | OpenAleph related-party graph screening — Phase 2 of the percolation build-out, the headline use case from [openaleph/openaleph#105](https://github.com/openaleph/openaleph/issues/105): every person and entity in the assembled BODS graph is screened against the OpenAleph instance in **two** percolation calls (vs one HTTP search per name per source in the OpenSanctions/EveryPolitician and ICIJ passes). New `opencheck/openaleph_check.py` — `assess_openaleph_names()` — gathered concurrently with `assess_cross_source_names` / `assess_icij_names` in the risk stage. The two calls encode the Phase 96 live findings: persons broad (`filter:schema=Person`, measured high-precision — a sanctioned oligarch lights up across eight watchlist collections, non-notable executives return nothing) and entities topic-scoped (`_WATCHLIST_TOPICS` = exactly the topics the signal ladder can act on; unfiltered entity percolation measured drowning in near-duplicate registry records). Per-hit `surface_forms` (the exact input phrase that fired) attribute each match back to the BODS `statement_id` bearing that name, then the **shared** cross_check machinery gates and classifies: 0.88 name similarity against the hit's own FtM names (alias-aware — a `percolator_match: other_name` hit is scored against the alias that actually fired), the single-token person guard, birth-year compatibility, and the imported topic ladder (direct sanction > debarment > sanction-linked > PEP; entities never PEP) → `RELATED_*` signals with `source_id: openaleph`, so the existing risk chips and 315° NW graph badges work with zero frontend changes. OS+OA duplicate signals for the same node are deliberately kept, as with OS+EP today. Gated matches whose topics map to no signal — `poi`, `corp.disqual`, leak and court collections — are never discarded: a `screening` out-collector carries them as `openaleph_screening` on the `risk_signals` event and both response models, rendered as a new informational "Archive matches — OpenAleph" section (name-derived, never identifier corroboration). Degradation follows the issue #50 contract: a missing `OPENALEPH_API_KEY` in live mode or a percolation call that could not run yields `DegradedSource` records (counts only, never names) — an empty result never passes for a clean screen. 16 new backend tests; suite 2,689. Frontend: tsc clean, vitest 134, production build clean. Commit `c1742ca`. |
| 98 | Sanctions: ownership chain split from adjacency — OpenSanctions [now distinguishes](https://www.opensanctions.org/articles/2026-08-13-sanction-control/) `sanction.control` (a direct or indirect subsidiary, asset or vessel of a designated party, at any stake and any depth, with an end-dated shareholding stopping the chain) from `sanction.linked` (plain one-hop adjacency), and OpenCheck did not. A `startswith("sanction")` catch-all — duplicated verbatim across `risk.py`, `cross_check.py` and `openaleph_check.py`, on the reasoning that each module should be readable in isolation — funnelled every subtopic into the softest bucket, so a company 52% owned by a sanctioned person rendered as amber "Sanctions-linked", medium confidence, over copy reading "the record is not itself sanctioned". True, and the exact case an ownership-and-control test (OFAC's 50 Percent Rule and its EU/UK equivalents) exists to catch. The taxonomy now lives once, in `risk.py`: four frozensets and a `classify_sanction_topics()` returning direct / control / linked / **unknown**, with all three modules ranking the same classification instead of each re-deriving it. New `SANCTIONS_CONTROLLED` and `RELATED_SANCTIONS_CONTROLLED` signals sit between `SANCTIONED` and `SANCTIONS_LINKED` (graph severity 7 / **6** / 3) at **high** confidence — the ownership assertion is deterministic, since upstream walked the chain; what is uncertain is the legal threshold, which the summary states outright rather than discounting into the confidence dot. Two suppression rules, because the two facts differ in kind: `sanction.linked` is a declared *superset* of `sanction.control`, so the weaker chip is dropped as the same fact stated less precisely, while a direct listing is a genuinely separate fact — an entity both designated *and* inside another designated party's ownership chain reports both. The related-party ladder is now direct > control > debarment > adjacency > PEP, shared by the cross-source and OpenAleph screens. The fallback for an unrecognised `sanction.*` subtopic is unchanged — still adjacency, never a listing we can't confirm — but it now logs, and a drift canary asserts every published sanction-family topic is explicitly classified: the test that would have caught this, since `sanction.control` was already inside `_RISK_TOPICS` and those entities were being fetched all along. Retrieving a topic is not the same as understanding it. Live-verified against OpenSanctions — [ru-inn-5005053446](https://www.opensanctions.org/entities/ru-inn-5005053446/) carries both topics, is not itself designated, and is 52% held by a sanctioned person. Also corrects two lines in `docs/risk-signals.md` that claimed `SANCTIONED` fires on any topic starting with `sanction`, wrong since the original direct/linked split. 15 new tests; suite 2,704 backend, 140 frontend. Commits `2488c1c`, `00a7946`. |
| 99 | Declared source liveness — three clocks, not one call to `date.today()`. Every BODS statement OpenCheck emitted asserted `retrievedAt` = the moment the mapper ran (`datetime.utcnow()` at `mapper.py:521`), `statementDate` = today, and `publicationDate` = today. Four different provenance claims collapsed onto one timestamp, and it was wrong for all but a genuine live call: a `bods_uk_psc` Parquet snapshot, a `cac_nigeria` fixture committed to the repo, a fourteen-minute-old replay-cache hit and a stub all claimed to have been downloaded at that instant. The [BODS dates guidance](https://standard.openownership.org/en/main/standard/modelling/dates-guidance.html) is unusually firm here — `source.retrievedAt` applies "only where data is being republished", and a republisher **must** state when it downloaded the data — so this was a conformance gap, not a presentational one. New `opencheck/provenance.py` records where a payload came from at the two chokepoints every adapter already passes through — `Cache` reads (`curated` for committed demo fixtures, `cached` with the entry's real age for the runtime cache) and `build_client()` construction (`live`) — so 38 adapters needed no changes; only the three that read neither HTTP nor cache declare themselves explicitly (`bods_uk_psc` / `bods_gleif` as `snapshot`, `cac_nigeria` as `curated`). Resolution is deliberately pessimistic: a Companies House lookup issues several requests, some cached and some live, and the bundle is only as fresh as its stalest component, so the **worst** liveness and the **oldest** timestamp win. The recorder lives in a `ContextVar` and each source is dispatched in its own asyncio task, which copies the context on creation, so concurrent fetches cannot bleed into each other. `_source_block()` now emits `retrievedAt` **only** where a retrieval was actually observed — stub output carries none at all, and a committed fixture with no declared harvest date carries none either, because a checked-out file's mtime records when git wrote it to that machine, not when the data left the register (`cac_nigeria` is the exception: its index declares `meta.harvested`, a real date, so it reports one). `statementDate` falls back to the retrieval date before today's, which for a months-old snapshot is far closer to the truth and is covered by the guidance's consolidation reading; pre-extracted Open Ownership subgraphs report `snapshot` with OO's own `publicationDate`. `SourceHit` gains `liveness` / `retrieved_at`, defaulting to `stub` so a source that declares nothing under-claims rather than over-claims; `LookupResponse` and the `risk_signals` event gain `source_liveness`, riding beside `degraded_sources` on the same principle — data that is not current must not read as live, just as a check that could not run must not read as clean. The replay cache needed no change (it stores completed events verbatim, so provenance stamped at fetch time travels with them) but is now pinned by a test, since the failure mode — a replayed bundle re-deriving `retrievedAt` at serve time — is invisible until it happens. Frontend: a `LivenessBadge` on any card that is not a fresh live call, with snapshots past 120 days warming to orange; `live` stays unmarked, because badging it tells a reader nothing they had not assumed. Also replaces the `datetime.utcnow()` deprecated in Python 3.12. 67 new backend tests (incl. canaries that fail if the clocks are ever re-collapsed) and 9 frontend; suite 2,771 backend, 149 frontend. Commit `33cccbd`. |
| 100 | `statementDate` from the register's own declaration date — GLEIF, Companies House PSC, SEC EDGAR. Phase 99 separated the four clocks and gave `statementDate` a sane fallback (the retrieval date, not today); this wires the first three sources that publish a declaration date of their own, so those statements say when the **register** asserted the fact. It is mostly a correction rather than an addition: all three dates were already being parsed — GLEIF `registration.lastUpdateDate`, Companies House PSC `notified_on`/`ceased_on`, the SEC 13D/13G filing date — and all three were being passed as `publicationDetails.publicationDate`, which BODS defines as the date *this* statement was published by the publisher named in that same block. That publisher is OpenCheck. A PSC notified in 2016 was therefore emitting a statement OpenCheck claimed to have published in 2016, ten years before OpenCheck existed. Open Ownership's own bundles model it the right way round (`statementDate` 2024-06-06 from the source, `publicationDate` 2025-02-28 from OO), and now so does OpenCheck. Two other sites were passing something worse still: Companies House officer `appointed_on` and the INPI role `start_date` — an interest **start** date, the first clock, copied into the fourth. Those are removed rather than promoted, because neither register publishes a per-officer notification date, so there is no declaration date to use and the retrieval-date fallback is the honest answer; the appointment date is not lost, it remains `interests[].startDate`, which is the clock it always belonged to. GLEIF's Level 2 relationship statements take the **subject's** `lastUpdateDate`, not the parent's: GLEIF's `/direct-parent` endpoint returns the parent's Level 1 record rather than the relationship (RR) record, so the RR's own update date is not available to us, and the Level 2 relationship is reported by the subject anyway — the parent's entity statement still carries the parent's own date. SEC issuer details take the most recent filing date, since the issuer block is read off the filings. For GLEIF this also part-answers the Time Machine's open item (a), "letting `statementDate` reflect each modification's date rather than now" — `lastUpdateDate` moves with each LEI record update; `recordStatus` (item b) is untouched. 24 new tests including a two-way canary per source — the register's date must reach `statementDate`, and must never appear in `publicationDate` — plus the corrected `test_ceased_pscs_emit_closed_record`, which had been pinning the old behaviour. libcovebods validation green. Suite 2,794 backend, 149 frontend. Commit `a8559ad`. |
| 101 | Register-supplied `statementDate`, second wave — and three registers that cannot supply one. Phase 100 wired GLEIF, Companies House PSC and SEC EDGAR; this sweeps every remaining adapter for a genuine record-level declaration date and wires the seven that have one. The headline pair is `bods_gleif` / `bods_uk_psc`: Open Ownership's published bundles carry **their own** `statementDate` (the source's declaration date, distinct from OO's `publicationDate` — the exact model Phase 100 cited as precedent), the flattened Parquet keeps it as a `statementdate` column, and OpenCheck was reading every neighbouring column while discarding that one, so those statements shipped with **no `statementDate` at all**. Now passed through verbatim: re-deriving it would replace a real declaration date with our processing date. Four more were already parsed and simply never threaded — `krs_poland` `dataOstatniegoWpisu` ("date of the last entry", not `dataRejestracjiWKRS`), `ur_latvia` officer `last_modified_at`, `ted_eu` notice `publication-date` (latest wins, same precedent as SEC issuer details) — and two needed a live probe to settle: `ares` `datumAktualizace` and `brreg` `rollegrupper[].sistEndret`, both **verified against the live APIs on 2026-08-14** and both previously dropped on the floor (brreg's role-group dict was discarded except for `type`). Latvia also had the Phase 100 error *inverted*: `last_modified_at` was a fallback for `interests[].startDate`, putting a declaration date into the "when was it true" clock. It now feeds `statementDate` only, and an interest with no `registered_on` correctly carries no start date rather than borrowing one. **Three of the four registers originally proposed for this phase turned out to have nothing usable**, which is the more useful finding and is now documented in `docs/sources.md` so it is not re-litigated: Estonia's scraped printable page exposes only `Registered`, a founding date; Denmark's Datafordeler CVR *is* bitemporal but the adapter's six GraphQL queries request only `virkningFra`/`virkningTil` — validity time, not transaction time — so there is nothing to wire until the queries change; and Brazil, probed live against both OpenCNPJ and BrasilAPI, returns no update stamp at all, its `data_situacao_cadastral` being the date the cadastral *status* took effect, a trap rather than a declaration. Deferred with reasons: `firmenbuch` collapses `EINTRAGUNGSDATUM` (register entry) and `EINTRITTSDATUM` (entry into office) into one field and no fixture carries either attribute, so splitting them needs a live sample first; `rpvs_slovakia` `PlatnostOd` and `rpo_slovakia` `validFrom` are version-validity windows, not transaction time. 18 new tests including the two-way canary per source — the register's date must reach `statementDate` and must never appear in `publicationDate`. Suite 2,812 backend. Commit `bf9714b`. |
| 102 | Beneficial ownership is asserted, never inferred. BODS distinguishes three states for `interests[].beneficialOwnershipOrControl` — `true`, `false`, and **absent** ("not stated") — and OpenCheck was collapsing the third into the first in five places, by reasoning from the *shape* of the interest rather than from anything a source had said: OpenCorporates network relationships inferred it from `interest_type in ("shareholding", "votingRights")` **and** hard-coded `True` on both percentage branches (the audit that found this phase spotted only the first of the three); New Zealand inferred it from `kind == "person"`; SEC EDGAR hard-coded `True` for every 13D/13G filer. A shareholding is a *legal* holding — whether it is also a *beneficial* one is a separate fact only a register or a BO declaration regime can supply, and equating the two is the error the nominee concept exists to name. The reasoning was already written down in this very file for the FollowTheMoney path (`_FTM_BO_ASSERTING_DATASETS`: "otherwise leave the flag unset ('not stated') rather than asserting a registered holding is a beneficial one"); it had simply never been applied to the commercial-register mappers. The policy now lives once, as `_BO_ASSERTING_SOURCES` + `set_beneficial_ownership()`, with an explicit source classification instead of ~55 scattered literals: an explicit `true`/`false` from a register is always passed through (a register saying "not beneficially held" is information, not silence), silence from a BO-publishing source still asserts, and silence from anything else omits the key. SEC EDGAR gets the sharpest fix, because the evidence was already in the bundle and simply never read: a 13D/13G "beneficial owner" is an SEC-rules term meaning voting or dispositive power, so an investment adviser voting client shares qualifies without any economic interest — `typeOfReportingPerson` codes `IA`/`BD`/`IC`/`EP`/`SA`/`BK` now make no beneficial claim, the capacity is **explained in `details`** rather than silently dropped, and the sole-versus-shared voting split (parsed since the adapter was written, never consumed) is surfaced too. Direction of error matters here: over-claiming beneficial ownership is a reputational assertion about a named person, and it travels into RDF, FtM, Senzing and BigQuery exports well beyond any caveat the UI can attach. Impact measured rather than assumed — the 1,264 interests in the stored Open Ownership bundles are passed through verbatim and unaffected; the frontend reads the flag only for graph sort ordering (`? 1 : 0`), where absent behaves as false. 22 new tests including a no-inference canary that walks the mapper output. Suite 2,834 backend. Commit `f5333e1`. |
| 103 | Provenance annotations — recording what the register said, not just what OpenCheck read. The mapper transformed a great deal and recorded none of it: a reader could see `otherInfluenceOrControl` with no way back to the Companies House code it came from, or a `birthDate` of "1975-08" with no way to tell whether the register published only a month or OpenCheck truncated a full date. New `bods/annotations.py` builds BODS v0.4 `annotations` entries — RFC6901 pointers (properly escaped: `~`→`~0`, `/`→`~1`, or a field name containing either silently addresses the wrong fragment), the `motivation` codelist, and a `validate_annotations` that catches the one failure the JSON schema cannot — a pointer that resolves to nothing validates fine and is useless to a consumer. **Two findings inverted the planned scope.** First, BODS already models date imprecision exactly where it occurs: `birthDate` legally accepts `YYYY` / `YYYY-MM` / `YYYY-MM-DD` *because* registers like Companies House publish month and year only, deliberately, for privacy — so rounding it, as the ticket originally proposed, would fabricate a day the register withheld on purpose. The strict fields (`foundingDate`, `dissolutionDate`, `startDate`, `endDate`, `statementDate`) do require full dates and sanction rounding, but no adapter emits a partial value into one — verified across the stored bundles and now pinned by a canary, with `round_partial_date()` plus a rounding annotation ready for the day one does. So the phase *records* precision rather than changing it: an imprecise `birthDate` carries a `commenting` annotation stating that the source never disclosed the rest, which is the distinction a consumer cannot otherwise draw. Second, the real loss was **vocabulary, not dates**: Companies House nature-of-control codes are deliberately not modelled as BODS interest types (nominee arrangements need an intermediary `arrangement` entity, not yet implemented), so the code identity survived only inside an English prose descriptor — which is precisely why the `NOMINEE` risk signal depends on the word "nominee" appearing in a sentence. Each interest now carries a `transformation` annotation naming the register's own code, machine-readably, without pre-empting the arrangement modelling. The design rule, applied everywhere: **the statement always carries the usable value; the annotation always carries the register's words** — `transformedContent` is defined as the post-transformation representation, which read literally would put the original in the target field, unworkable for a `YYYY-MM-DD` field that cannot hold "01 November 2018"; worth raising upstream with Open Ownership. Only lossy or non-obvious transformations are annotated, and the cost is measured rather than hoped for: +40% on a deliberately annotation-dense fixture, pinned below 75% by a test, with real bundles dominated by Open Ownership statements that pass through verbatim and carry none. Output validates against libcovebods with zero errors. Also ships `docs/dates.md`, owed since Phase 99 — the four clocks, which sources supply a declaration date, the three registers that have none, and how precision is recorded. 29 new tests. Suite 2,863 backend. Commit `e67ae55`. |
| 104 | The nominee signal fires on structure, not on a sentence. `NOMINEE` — AMLA CDD RTS condition (c) — worked by substring-matching the word "nominee" in a name or a free-text descriptor. It fired correctly on UK Register of Overseas Entities filings, but **by accident**: the mapper renders each nature-of-control code into an English descriptor in `interest.details`, and those descriptors happen to contain the word. A register stating the identical fact in a code, a boolean, or another language was invisible to it — and if Companies House ever reworded a descriptor, the signal would have gone quiet with no test failing. Companies House publishes `natures_of_control` on every PSC record and six ROE codes state a nominee arrangement outright; `assess_bundle` already receives the raw adapter payload, so those codes are now read at full fidelity rather than recovered from prose OpenCheck itself generated. Note the input is the **raw payload, not the Phase 103 annotations**: those carry the code inside a human-readable sentence, and parsing a code back out of a description string would have been a more elaborate version of the bug being fixed — the annotations make the code visible to *consumers of the export*, the raw path makes it available to *the engine*, and both are right. The two grades of evidence are now distinguished rather than conflated: a filed code reports `basis: "structured"` at **high** confidence with the code itself in the evidence, so a reviewer can check the filing; a text match reports `basis: "textual"` at **medium**, because "Nominee Services Ltd" is a company name, not a declaration, and the summary says outright what it matched on. Structured wins where both would fire, since the mapper renders the code into `details` and the same fact must not be reported by its weaker trace. Ceased PSCs are excluded — that arrangement has ended and is not a live risk. `NOMINEE_NATURE_CODES` is derived from the vendored enumeration rather than hand-written, with a drift canary asserting no published nominee code goes unclassified. It lives in a new `bods/nominees.py` rather than beside the enumeration itself: `bods/psc_natures.py` is auto-generated from Companies House's `psc_descriptions.yml` and says "Do NOT edit by hand" in its docstring, so a derived constant added there passes locally and then fails the vendored-enum drift check when the generator rewrites the file. That happened on the first attempt; `tests/test_generated_modules_not_hand_edited.py` now catches it in `make test` rather than in CI, since the drift job needs the network and only runs remotely. The textual fallback is unchanged for every source that publishes only prose, so recall does not regress; what changes is that a prose match no longer claims the confidence of a filed fact. Deliberately **not** done: modelling nominee arrangements properly as BODS `nominee`/`nominator` relationships through an intermediary `arrangement` entity — `mapper.py` defers that, and this phase does not pre-empt it. 22 new tests. Suite 2,887 backend. Commits `c5d975d`, `3ca54b1`. |
| 105 | Counter-sanctions are not sanctions. OpenSanctions has published `sanction.counter` as a distinct topic — ["designations imposed by countries with weak democratic institutions … often punitive responses to foreign sanctions, or … used to suppress domestic political opposition"](https://www.opensanctions.org/docs/topics/) — and OpenCheck classified it correctly and then threw the distinction away: `_DIRECT_SANCTION_TOPICS` held `{"sanction", "sanction.counter"}`, so both collapsed into one `SANCTIONED` chip reading "OpenSanctions lists this record as sanctioned". This was not the Phase 98 catch-all bug; it was a deliberate call, recorded in the comment "still a direct listing of the entity", and on that narrow point it was right — the entity *is* the subject of the listing. The error was treating structure as meaning. Found on [Liverpool FC](https://opencheck.world/?lei=549300TFSFK0CL2F3J50), where a connected person name-matched [a Wall Street Journal correspondent on Russia's MFA retaliation list](https://www.opensanctions.org/entities/ru-mfa-6748b783747445ce8f29d8f3f94572517d490e96/) and rendered in the same rose as an OFAC designation — a listing whose *cause*, for much of that list, is journalism or sanctions enforcement, reported by a due-diligence tool as adverse. `SanctionTopics` gains a fifth field, `counter`, kept out of `direct` precisely so no call site can merge them by accident, and new `COUNTER_SANCTIONED` / `RELATED_COUNTER_SANCTIONED` signals carry it. Three deliberate choices. **Confidence stays `high`** — the listing is a fact, and discounting it into the confidence dot would misreport certainty about the *data* to express a judgement about the *regime*; the summary states the judgement in words instead, naming what the designation is not ("not a designation by an EU, UK, US, UN or other mainstream sanctions authority"). **Severity is 2, below plain adjacency**, and the palette is slate — outside the rose/amber sanctions ramp entirely, since the whole failure was a counter-designation reading as a shade of "Sanctioned"; a vitest assertion now fails if either chip drifts back into `rose-*` or `amber-*`. **It is emitted last** in the related-party ladder (direct → control → debarment → adjacency → PEP → counter), inverting the structural ranking on purpose: consumers take `[0]` as the headline finding, so a record both OFAC-designated and counter-listed must lead with the former. Both signals fire together when a record carries both topics — neither substitutes for the other. The drift canary needed no change, which is the point: `sanction.counter` was inside `_KNOWN_SANCTION_TOPICS` all along, so no test could have caught this. Recognising a topic is not the same as knowing what it means — Phase 98 learned that a topic can be fetched and misunderstood; this phase, that it can be classified and still misreported. 5 new tests (4 backend, 1 frontend). Commit `862d436`. |
| 106 | A name is not an identification. Phase 105 fixed how the Liverpool FC hit was *described*; this fixes whether it should have attached to that person at all. A connected "Michael Gordon" name-matched Michael R. Gordon, a Wall Street Journal correspondent on Russia's MFA retaliation list, and was reported as a related-party finding. Measuring the gate rather than tuning it produced the governing fact: `Michael R. Gordon` vs `Michael E. Gordon` — definitively different people — scores **0.9375**, *above* the 0.9333 of the genuine abbreviation pair. A middle initial is two characters, so it barely moves a character-similarity score; there is no threshold that separates them, and raising the gate drops true positives first. A second problem compounds it: `Michael R. Gordon` passes at 0.9333 while `Michael Alton Gordon` fails at 0.8235 — the difference is whether the register abbreviated, a fact about the register, not the person. So discrimination now comes from a second attribute. `corroborating_attributes()` reports which of birth year (±1) and nationality agree, requiring **both** sides to supply it — the asymmetry that mattered, since the MFA record publishes no birth date and `_birth_year_compatible` returned `True` for exactly that reason ("hit has no DOB — keep"). Knowing our own side's date was being read as agreement; silence is not corroboration, the same shape as the `degraded_sources` lesson one level down. Person signals are capped when nothing but the name agrees — never `high`, since "high confidence this is the same person" is the one claim a name alone cannot support — but the cap is deliberately **two-tier, not a flat `low`**: sanctions screening *is* name-based, and an exact match against a designated person is something a reviewer must adjudicate, so filing every such hit under `low` would misreport standard practice as a weak lead. Exact-but-uncorroborated is `medium`; the fuzzy band, where Gordon sits, is `low`. The prose does the rest of the work regardless of rung — every uncorroborated person match now opens "Possible name match only" and names what is missing, so it can no longer read as an assertion. Entities keep the score-only ladder: an organisation name is distinctive in a way a personal name is not. The OpenAleph percolation screen imports the same two functions rather than keeping a second copy, so the screens cannot diverge. A canary pins the score inversion itself and fails if it ever reverses — at which point a threshold fix becomes viable and this rule can be revisited. 5 new tests. Commit `57adc39`. |
| 107 | RDF had the annotation vocabulary all along and never used it. Phase 103 began emitting BODS `annotations` — the Companies House nature-of-control code behind an `otherInfluenceOrControl`, a note that a `birthDate` was published imprecise rather than truncated by OpenCheck — and the RDF export dropped every one. Not for want of vocabulary: `bods/rdf.py` already emitted `Annotation`, `annotation`, `motivation`, `description`, `statementPointerTarget`, `creationDate`, `createdBy` and an `Agent`. The machinery was built in Phase 81 and wired to one input — `_add_annotations()` took risk signals, possibly-same pairs and degraded sources, OpenCheck's own conclusions, and never read `stmt["annotations"]`. So the predicate the standard defines for the register's words was in use for OpenCheck's, and the register's were absent. **The two now share an emitter and are separated by graph, not by predicate**: statement annotations go in the statement's own named graph, analysis annotations stay in `opencheck.world/analysis/…`, so a consumer can ask for either without getting the other — an initial reading of this as a "predicate collision" was wrong, and the named-graph split Phase 81 already had is the right answer. Pointers travel verbatim rather than being re-derived, since Phase 103's RFC6901 escaping (`~`→`~0`, `/`→`~1`) was the part of that phase hardest to get right; `transformedContent` is added, the one vocabulary term Phase 103 used that RDF never emitted; and `createdBy` now carries the annotation's own agent rather than a hardcoded "OpenCheck risk engine". **Also corrects a namespace wrong since Phase 81.** `CODES` pointed at `https://standard.openownership.org/codelists#`, taken in good faith from the `@prefix codes:` line in the published 0.4.0 Turtle. The [quick reference](https://vocab.openownership.org/pages/3_quickref.html) states the rule as "capitalise the code and prefix it with the namespace `https://vocab.openownership.org/codelists#`", and Open Ownership confirm the `.ttl` declaration is an error in that file. Every codelist term OpenCheck has emitted in RDF — entity types, `directOrIndirect`, annotation motivations — carried the wrong namespace, **including in the published Estonia `.nq.gz` corpus, which now needs regenerating**. The existing "no accidental third namespace" guard caught the change on the first run and now lists only the corrected namespace, so a revert fails. Casing was already right. Deliberately unchanged: **Senzing and FtM still drop annotations**, which is correct — neither model has anywhere to put them — and BigQuery already round-tripped them in an `annotations_json` column; the ticket's "all three exports" framing narrowed to one on inspection. Cost is pinned by triples-per-annotation rather than a size ratio, because a ratio measures the fixture: the same annotation is +45% on a real Companies House statement and +107% on a sparse test one. 6 new tests. Commit `6396079`. |
| 108 | The register's own words, finally visible. Phase 103 began emitting BODS `annotations` — the Companies House nature-of-control code behind a rendered `otherInfluenceOrControl`, a note that a `birthDate` was published month-and-year-only rather than truncated by OpenCheck — and Phase 107 carried them into RDF. Neither put them in front of a user: `grep -i annotation` across `frontend/src` returned nothing but export-panel prose about a different mechanism. The data arrived in the browser on every lookup and was discarded at the last step, which for a tool whose pitch is showing its working is the one place it most needed not to be. The results page now marks any transformed value with a dotted underline and offers a persistent **"as filed"** toggle: on, the register's words lead and OpenCheck's reading follows in muted text — that order deliberately, since the point is showing *whose* vocabulary you are reading. Three decisions worth recording. The state is **module-scoped, not per-card**: a lookup renders many source cards, and setting "as filed" on one while the next still shows OpenCheck's vocabulary would read as a bug, so one setting applies wherever annotations exist. It **defaults to OpenCheck's reading**, because that value is always present and always machine-readable, where the register's words exist only for lossy transformations. And the toggle **renders only where a bundle carries annotations** — most sources annotate nothing, and companies served from a stored Open Ownership bundle bypass the mapper entirely (accepted as scoping, not a bug), so an always-present control would frequently change nothing. Pointer matching is **exact, never prefix**: an annotation on `/recordDetails/interests/0/type` describes that interest, and letting it match a parent or a sibling would attribute the register's words to something it never said — the case that gets its own test. The `~1`-before-`~0` unescaping order is pinned too, since a field named `a~1b` encoded as `a~01b` silently addresses `a/b` if reversed. Also corrects two pieces of copy that Phase 107 made stale: the export panel and the API-page format list both described `bods:Annotation` as carrying risk signals in an analysis graph, true but no longer complete now that the register's words ride in each statement's own graph — the same hardcoded-claims drift as the Phase 98 share card, prose asserting what the engine produces and going quiet when it changes. 20 new frontend tests. Commit `ed448cf`. |
| 109 | An unbadged node is a claim, and it was the wrong one. On the Rosneft page the risk panel flagged a related party as sanctions-linked; scroll to the OpenSanctions source card, find the same node in its ownership graph, and it carried no badge at all. Nothing was misclassified — the two graphs are simply fed different lists. `RELATED_*` signals are assessed late in `_lookup_pipeline` against the **merged** bundle and ride on the top-level `risk_signals` event, while a source card's graph is handed a `/deepen` response carrying only that source's own findings. A cross-source finding therefore could not reach a per-source graph, and in a due-diligence tool an unmarked node does not read as "out of scope", it reads as checked and clean. That is the wrong inference to invite, and PR #115 made it more frequent by reporting every applicable signal on a related-party path rather than one per hit. The fix is a filter, not a wider pipe: new `frontend/src/lib/signalScope.ts` scopes the lookup's top-level signals to a given bundle, keeping one only when its code starts with `RELATED_` **and** its evidence lands on a `statementId` the bundle actually contains. Three decisions are worth recording. First, `buildSignalMap()` **moved out of** `BODSGraph.tsx` into the same module and the filter is built on it — two copies of "which node does this evidence point at" drifting apart is precisely the class of bug being fixed, so there is now one. Second, the scope is `RELATED_*` **only**, and a test pins that: `COMPLEX_OWNERSHIP_LAYERS` carries a `longest_path` computed over the merged graph, and a single source's bundle usually holds a fragment of it, so scoping it in would assert a structural claim untrue of the graph on screen — the same error pointed the other way. Third, the badge does not arrive unexplained; where scoping contributes anything the card says so, naming the count and that it came from screening the network against other sources rather than from this register. `SubsidiaryNetwork` — the third `BodsGraphExplorer` render site, suspected in the ticket — had the same gap and worse, passing no signals at all; it now receives them from `FullCheckPanel`. 17 new frontend tests. Commit `6686cda`. |
| 110 | Counting what the sources actually contribute. "Is OpenAleph screening contributing anything in production?" was, until now, unanswerable without running lookups by hand and counting — and the obvious alternative, sweeping `/lookup` over a few hundred LEIs, is both expensive and misleading. Every lookup is a live fan-out across ~38 upstreams, so a sweep would load a free-tier instance, risk tripping rate limits whose **degraded** results read as "signal absent" (the trap that produced two retracted findings during Phase 98 testing), pull CC BY-NC data at volume for what is effectively analytics, and sample whichever LEIs happened to be chosen. Server-side counting has none of those problems: it observes traffic that was going to happen anyway, is exact rather than sampled, and keeps working without anyone re-running anything. New `backend/opencheck/signalstats.py` counts signals per `(code, source_id)`, `degraded_sources` per `(source_id, check, reason)` — a signal count without the count of screens that failed to run is not a low number, it is an unknown one — and completed pipeline runs as the denominator, exposed at `GET /signalstats` on the same contract as the existing `/memstats`: public, unauthenticated, `no-store`, no rate limit, aggregate only. Three decisions worth recording. The counting sits **inside** `_merge_signals` rather than at its call site, so "count after dedup" is true by construction — that function is where the rules deciding what a *distinct* signal even is live, and since PR #115 related-party paths emit several signals per hit, so pre-dedup numbers would overstate. It is **opt-in** via a `record_as` argument defaulting to `None`, because `_merge_signals` has a second caller: `/report`, a hand-run free-text debugging endpoint whose traffic would inflate the per-lookup denominator and corrupt the one ratio the instrumentation exists to produce; opt-in also means a future caller cannot skew the numbers merely by existing. And privacy is **structural rather than policed** — the recorders read only `code` / `source_id` / `check` / `reason`, never `summary`, `hit_id`, `evidence` or a degradation's free-text `detail`, so entity names, LEIs and related-party names are unable to reach a counter rather than merely forbidden from it; the tests stuff names into every free-text field a signal has and assert none of them appear, then check the same end-to-end through a real lookup. Two limitations are recorded rather than hidden: `lookups` counts pipeline runs, not sessions (replays are served from cache and never reach the pipeline), and the counters are in-process, so they reset on deploy and on Render spin-down — making them durable means scraping the endpoint periodically, which is a separate decision. A cardinality cap turns a key-widening defect into a visible `truncated` flag instead of unbounded memory. 14 new backend tests. Commit `97e0925`. |
| 111 | Being foreign is not a risk finding. Every UK company with an LEI carried a "Non-EU jurisdiction" chip at **high** confidence — the same rung as `SANCTIONED` — and three of the six homepage examples advertised it, so BP was flagged for being British and Eli Lilly for being American, next to genuine adverse findings. Reading AMLA's draft CDD RTS (Article 28(1), consultation closed 8 May 2026) against what OpenCheck actually did found the claim has no basis in the instrument it cites. The **only** operative "outside the EU" provision is Article 12(1)(b), a sub-condition of the complex-corporate-structure definition, and the parent regulation is more explicit still: AMLR **Annex III(3)** enumerates higher-risk geography as *qualified* categories of third country — FATF-listed, no effective AML/CFT system, significant corruption, sanctioned, terrorist financing, financial secrecy — while **Annex II** places third countries with effective systems in the **lower**-risk column. "Being a third country" appears in neither. The RTS's actual model for third countries is an equivalence test (Articles 17(a), 22(b)): a regime "no less robust" than the AMLR is treated identically to a Member State. The EEA references that prompted the question turn out to be Article 31(k), about e-money issuers geo-fencing IP addresses for supervisory exemptions — nothing to do with ownership chains. **Two correctness bugs fell out of the same reading.** Article 12(1) requires three or more layers "and, in addition, **more than one** of the following conditions" — at least two — and the composite fired on one, so any three-layer group containing a single non-EU entity was reported as meeting an AMLA threshold it did not meet; and condition (b) is scoped to entities "present at any of **these layers**", where the rule scanned the whole bundle, so an unrelated side-branch entity counted. Condition (d) is still deliberately excluded: its "no legitimate economic rationale" limb cannot be judged from data, `POSSIBLE_OBFUSCATION` already mirrors it advisorily, and leaving it out lets the rule under-fire rather than over-assert. `NON_EU_JURISDICTION` is now `kind="context"` — a new field on every signal separating structural observations from adverse findings — at low confidence, relabelled "Outside EU/EEA", slate rather than warning-orange, in its own "Structural context" section. The field lives on the signal because the risk **count** is rendered independently by the results page, the OG share card and the share-page meta description, and three hand-kept exclusion lists is precisely how the curated homepage claims drifted in Phase 98; a missing `kind` reads as "risk" so cached payloads are untouched. That change surfaced a live narrative bug: the evidence packet gated its "the engine ran and identified nothing" fact on *any* risk item, so a company whose only signal was "reaches outside the EU/EEA" lost its absence-of-risk statement — exactly backwards for a clean company. Replacing what was removed, a new **`EU_HIGH_RISK_THIRD_COUNTRY`** signal reads the EU's own Article 29 list (Delegated Regulation (EU) 2016/1675, current to 2026/83 applying 29 January 2026; 23 Section I jurisdictions plus Iran and the DPRK, verified against the EUR-Lex consolidated text). Kept **separate** from the FATF codes rather than widening them, because the EU list is the legally decisive trigger for mandatory EDD where a FATF listing is not binding in EU law by itself, and because the two genuinely diverge — the Commission adopts updates months after the plenary that prompts them, so Algeria and Namibia are EU-listed but came off the FATF grey list in June 2026, and Myanmar is FATF black but EU Section I. A test pins that divergence so a future refresh cannot quietly collapse it. Also fixed: `NON_EU_JURISDICTION` collapsed globally on `(code,)` with the merge assigning rather than combining, so the last source processed won outright and every jurisdiction node found by earlier sources vanished from the graph overlay — reproduced directly, then keyed per source like the FATF signals it structurally mirrors. **Deliberately not built:** a jurisdiction-level signal for Annex III(3)(f)(iv), "not requiring beneficial ownership information to be recorded or held in a central database or register". It was the original recommendation and it is wrong three times over — FinCEN removed US domestic BO reporting on 11 August 2026, six days before this was written, so any shipped list is stale within weeks; the criterion turns on whether information is *recorded*, not published, so the common case of a register open to law enforcement but not the public **satisfies** it while being invisible to a public-data tool; and worst, the only observation available to us is "we found nothing", which conflates *no register*, *closed register* and *OpenCheck has no adapter yet* — turning our own coverage gap into an accusation against a country. The Annex II lower-risk allowlist is dropped for the mirror-image reason. The standing rule this establishes, now written into `docs/risk-signals.md`: **a geographic risk chip only where an authoritative, externally maintained, dated list says so.** The source catalogue also stopped annotating each new non-EU register with the signal — `cac_nigeria` was described as "Africa's first public beneficial ownership register" and tagged with a risk flag in the same sentence. Tracks a draft RTS; AMLA has not published all consultation responses, and its Article 32 geographical-risk guidelines are due by 10 July 2027. 15 new tests. Also unblocks CI: `test_every_public_route_has_a_rate_limit` had been failing on `main` since Phase 110 added `GET /signalstats`, which is deliberately undecorated — its docstring says so outright — but was never added to the test's `_EXEMPT_PATHS`, so the guard against shipping an accidentally unlimited endpoint was instead flagging a deliberate one. Commits `2621bad`, `071d43d`, `d3dc4ef`, `b25d315`, `188f557`, `b7cdea4`. |
| 112 | Walking every place that claims what the engine produces. Phase 111 changed the risk engine; this phase finds and fixes everything downstream that was still asserting the old output — and adds the guard that should have caught it. Four maps name signal codes (`og_image.SIGNAL_STYLE`, `RiskChip.RISK_PRESENTATION`, `BODSGraph.SIGNAL_STYLE`, the narrative packet's `_RISK_LABELS`) and **nothing checked any of them against the codes the backend can actually emit**. `RiskChip.test.ts` asserts every graph badge has a chip, but it starts from the frontend, so a code the frontend has never heard of passes in silence. The new `test_signal_label_coverage.py` enumerates codes from `risk.py`, `cross_check.py` and `securities.py` and asserts all four cover them, plus the reverse for `og_image` so a label for a retired code is caught too; the TypeScript maps are parsed as text, which is crude but the alternative — a hand-maintained list on the frontend side — is another copy of the thing that drifts. It immediately found two live defects. **`SANCTIONED_SECURITY` had no graph badge at all**, rendering through the fallback style as a grey "!" at severity 0 — a sanctions-family finding shown as the lowest-priority marker on the graph — and it was invisible to the guard for the same reason it was invisible to review: the code existed only as a string literal inside a function body, never as a module constant. Promoted, given the sanctions palette at severity 6, and the convention (`NAME == "NAME"`) is now documented as load-bearing rather than stylistic. **`_RISK_LABELS` had no entry** for `POSSIBLE_OBFUSCATION` or `SANCTIONED_SECURITY`, so the narrative prompt was handed a title-cased raw code — precisely what that prompt instructs the model never to print. The **dedup rule is now stated rather than inferred**: `_STRUCTURAL_SIGNAL_CODES` collapses on `(code,)` and the merge *assigns*, so membership is not "is this a structural claim?" but "does its evidence identify particular nodes?" — anything that names nodes loses all but the last source's, and with them the graph badges `buildSignalMap` would have drawn. `TRUST_OR_ARRANGEMENT` and `NOMINEE` both carry per-node `statement_id`s in `evidence.matches[]` and were moved out, joining `NON_EU_JURISDICTION`; reproduced first, with GLEIF's Stiftung at E1 losing to Companies House's nominee at E7. The alternative — merging the evidence arrays — was rejected because each summary is composed in `risk.py` from *that source's* matches, so merging evidence while keeping one source's prose would make a signal silently contradict its own evidence, and doing it honestly would mean duplicating rule logic into the router. `COMPLEX_OWNERSHIP_LAYERS` has the same loss via `longest_path` and is deliberately **not** moved: per source it would report different layer counts, and the chip strip picks its winner by confidence rather than depth, so the number shown would become arbitrary. A test pins both sides so neither is "fixed" the wrong way. Downstream, **the pre-baked curated summaries** — served to first-time visitors with no model call — were regenerated, but only after catching that Phase 111 changed the prompt wording and never bumped `PROMPT_VERSION`, which would have stamped post-change summaries with the pre-change version and made the two indistinguishable, the one thing that constant exists to prevent. All six had narrated non-EU status as a risk, **including Ørsted (DK) and Eesti Energia (EE) — both EU companies**, flagged because their chains reach non-EU subsidiaries; Eesti Energia's read "giving rise to high-confidence structural indicators for non-EU jurisdiction exposure" on the strength of a Jordanian subsidiary. Useful evidence that the old chip was mis-scoped well beyond UK companies. All six picker cards were stale too — not just the three carrying non-EU, since Rosneft and Ørsted had drifted independently — and were re-derived from the regenerated production packets rather than by reading the cards back, which is circular. `COMPLEX_CORPORATE_STRUCTURE` disappeared from **every** curated example, having fired throughout on layers plus a single condition; several `RELATED_PEP` confidences drop `high`→`medium`, which is Phase 106's name-match gate surfacing, not this change. Cards now order by graph severity then confidence — alphabetical ordering had been pushing `SANCTIONED` off Rosneft's card in favour of `DEBARMENT` — and show risk findings only, since the results page gives context signals a separate heading that a bare chip strip cannot convey. The share-card showcase moved from BP, which now yields two low-severity signals and made a thin advert for copy promising a count and a range of risk colours, to **Eli Lilly**, whose three signals span three colour families; Rosneft's nine were the alternative, but leading the homepage with a sanctioned state entity is a heavier editorial choice than the slot needs. Both renders passed the **full** signal list including the `kind="context"` entry, so the generator's own filter was exercised end to end — four in, "3 risk signals found" out. Also fixed: `test_every_public_route_has_a_rate_limit` had been red on `main` since Phase 110 added `GET /signalstats`, a route deliberately undecorated per its own docstring but never added to the test's `_EXEMPT_PATHS`, so the guard against an accidentally unlimited endpoint was flagging a deliberate one. 15 new tests. Commits `7d7d86d`, `30a7220`, `afa9442`, `ae0b541`, `08fe406`. |
| 113 | The depth shown depended on which source finished last. Two loose ends from the Phase 111/112 work, both of which turned out to be worse than the notes that logged them. `COMPLEX_OWNERSHIP_LAYERS` was recorded as a graph-badge problem: it collapses globally on `(code,)` and the merge *assigns*, so only the last-processed source's `longest_path` survived and nodes found by earlier sources lost their badges. Reproducing it showed the **headline figure itself was wrong** — the number of layers reported was whichever source happened to finish last, so a lookup where Companies House finds a five-layer chain and GLEIF finds three reports 5 or 3 purely on ordering, with a surviving `longest_path` that is not the chain justifying the number. A user reading "3 layers of ownership" had no way to know a five-layer chain had been found and discarded. The fix, however, is much smaller than the note assumed. That note said the depth needed computing once over the merged bundle — a restructure of the per-source pipeline. It does not: **statement ids are namespaced per source** (`_stable_id(source_id, …)`), so no ownership edge ever bridges two sources, `bods_all` is a concatenation of disjoint subgraphs rather than one connected graph, and its longest path **is** the maximum of the per-source longest paths. Verified directly rather than assumed. So a resolver on the existing global collapse — deepest chain wins — is *exactly equivalent* to the merged-bundle computation at a fraction of the risk, and the surviving evidence is now the chain that demonstrates the number. It is introduced as a named `_COLLAPSE_RESOLVERS` hook rather than an `if` buried in the merge, because "last one wins" is wrong for any code whose evidence carries a magnitude, and the next such code should have somewhere obvious to go. The three remaining whole-structure codes still collapse globally and must not be moved out — per source they would fire once per source with conflicting values, and the chip strip picks its winner by confidence rather than depth, so the number shown would become arbitrary in a different way. Second, **AMLA condition (a) is now scoped to the layered path**, closing an asymmetry Phase 111 left behind: point (a) reads "a legal arrangement or similar legal entity such as a foundation **in any of the layers**", the same scoping language as point (b), which Phase 111 already honoured — so a trust sitting on a side branch of the bundle had been counting towards the complex-structure composite. Point (c) is deliberately **not** scoped the same way: it reads "nominee shareholders or nominee directors involved **in the structure**", which is looser than "in any of these layers", and collapsing the two readings would erase a distinction the drafters made. A test pins that asymmetry so it is not later tidied into consistency. `_trust_condition_met` reuses `_trust_or_arrangement_signal` rather than re-implementing the legal-form keyword matching, so the condition and the standalone signal cannot drift apart on what counts as a trust; and as with (b), the standalone signal stays bundle-wide, because "this bundle contains a trust" and "a trust sits on the layered chain" are different claims and only the second one bears on Article 12. 4 new tests. Commit `f0973df`. |
| 114 | Declining to name a parent is not the same as having something to hide — and GLEIF never names people at all. Eli Lilly's page carried the "Opaque ownership" risk chip reading "Ownership chain contains: unknown person in ownership chain · Source: gleif", which is wrong three times over. The wording asserts something GLEIF cannot say — Level 2 relationship records only ever identify *entities* (LEI holders), never people. The classification ignored *why* no parent was reported: the LEI ROC policy defines a closed list of **permitted** reporting-exception reasons ([Reporting Exceptions Format 2.1](https://www.gleif.org/en/lei-data/access-and-use-lei-data/level-2-data-reporting-exceptions-2-1-format), and the [Reporting Exception Ontology](https://www.gleif.org/ontology/v1.0/ReportingException/)), and Eli Lilly's is `NATURAL_PERSONS` for both categories — "controlled by natural person(s) without any intermediate legal entity meeting the definition of accounting consolidating parent", the normal filing for a widely held issuer. And the chip never reached the GLEIF source card it came from, because `_opaque_ownership_signals` discarded the `hit_id` the caller passed. The rule fired on statement *types* (`unknownPerson` / `anonymousEntity`) that the GLEIF exception bridges use for every reason alike, so it could not distinguish "no parent exists under the accounting-consolidation definition" from "a known parent is deliberately withheld". Now the reason code drives classification. The four structural reasons — `NATURAL_PERSONS`, `NO_KNOWN_PERSON` (diversified shareholding), `NON_CONSOLIDATING`, `NO_LEI` — produce a new **`GLEIF_REPORTING_EXCEPTION`** signal, `kind="context"` like Phase 111's "Outside EU/EEA" chip, with per-reason wording and the exception's category/reason/legal-reference in evidence; `NO_LEI` gets distinct language because there a parent *does* exist and merely isn't identified in GLEIF. Unrecognised future codes degrade to context, never to a risk chip. `OPAQUE_OWNERSHIP` narrows to what a register actually asserts as withheld — the **`NON_PUBLIC`** family (plus its five deprecated pre-2022 variants) — and simultaneously widens to genuine opacity it had been missing: Companies House super-secure PSCs (`anonymousPerson` — the narrative label had claimed "super-secure" for a rule that never fired on one) and the unspecified-`interestedParty` reasons where a PSC exists but is unidentified or non-responsive (`noBeneficialOwners` and lawful disclosure exemptions deliberately excluded). Confidence rises to `high` throughout: every firing is now the register's own claim, not an inference from a placeholder type. The mapper aligns with the Open Ownership `bods-gleif-pipeline` conventions it already cites: per-reason bridge names ("Natural person(s) (GLEIF reporting exception)", "Undisclosed parent (GLEIF reporting exception)") instead of a uniform "Unknown parent"; `unknownEntity` for parents that are real but unidentified, reserving `anonymousEntity` for deliberate withholding; and OO-style `commenting` annotations ("This statement was created due to a NATURAL_PERSONS GLEIF Reporting Exception for …") on both bridge and relationship statements, which the Phase 107/108 annotation surfaces render for free. The two missing deprecated reasons (`LEGAL_OBSTACLES`, `DISCLOSURE_DETRIMENTAL`) joined the reason table; the invented `AUTHORITIES_DISCRETION` left it. Both signals carry `evidence.matches[].statement_id` pointing at the bridge node, so the existing `signalScope` machinery badges the graph, and the `hit_id` fix lands the chips on the GLEIF source card. `bods_gleif` (the OO bulk dataset) deliberately stays silent on placeholder types — the flattened rows no longer carry the reason, and a wrong chip is worse than none while the live `gleif` source covers the same ground with full reason codes. 15 new tests. Commit `149aa14`. |
| 115 | One pooled source for the EITI countries' beneficial ownership registers. The 2024 EITI Requirement 2.5 stocktake marks 15 countries' BO registers "publicly available"; live verification (18 Aug 2026) found only four worth pooling, and they ship as **one source, `eiti_bo`** — not one adapter per register — because the pooled universe is small and smaller still filtered to LEI holders (decision recorded on the Notion ticket). Each register needed a different sourcing approach, all offline (the cac_nigeria mould; none offers an API): **DRC** — the ITIE-RDC Registre des propriétaires effectifs, the only EITI BO register anywhere with a bulk download (54 companies / 202 owners via one-click XLSX; ownership %, voting rights, PEP flags; Loi n°25/048 du 1 juillet 2025) — with its two data traps handled at build time: `30/11/-0001` is a null-date sentinel, and share values mix fraction-of-1 and literal-percent semantics per filer, normalised by a per-company-sum heuristic with the raw value kept in a transformation annotation. **Armenia** — the State Register publishes per-declaration **BODS v0.2 JSON** (no auth; seed list = EITI Armenia's 27 declaring mining companies; declarations are current, most filed in 2026), which `map_eiti_bo` upconverts statement-by-statement to v0.4 — deterministic IDs, `interestLevel`→`directOrIndirect`, string shares→floats, register transliterations preserved, the upconversion recorded via annotations, and the declaring company gains the register-published `AM-REG`/`AM-TIN` identifiers. **Nigeria** — the committed CAC PSC harvest filtered to NEITI solid-minerals-covered companies, the filter evidence dated per record (the NEITI portal itself is frozen ~2023 and its oil & gas search returns HTTP 500 — it is evidence, never data). **Indonesia** — slot reserved; the AHU `getReportBo` API is in maintenance. Identity resolution runs once, offline, against full GLEIF country snapshots (AM: 80 records, CD: 35): registration-number equality first — Zangezur's `27.140.00009` matches its GLEIF `registeredAs` exactly — then normalised-name equality at lower confidence. The launch index is deliberately **LEI-only: 3 of 91 harvested companies resolve** (Zangezur Copper Molybdenum Combine, Dangote Cement, BUA Cement); DRC contributes zero because none of its 54 registered companies holds an LEI — the only DRC extractive LEI holder, Kamoto Copper Company, is not in the register export. The artifact manifest counts every unmatched company, so the gap is data, not silence — and a conference-ready finding about LEI coverage in extractives. Excluded from the pool: Tajikistan (pbo.eiti.tj asserts all rights reserved) and Trinidad & Tobago (register frozen ~2021). The adapter asserts only register-published identifiers (`am_regnum`/`am_tin`/`ng_cac_rc`/`cd_nif`), never the derived LEI; per-register licence notes (DRC and Armenia state none — included with attribution) ride in the manifest. Refresh = re-run the three harvest subcommands + `build`; when the EITI open data portal (CKAN, with Datopian, due ~Sept 2026) launches, evaluate swapping fetchers for portal sourcing. Commit `cc8455f`. |
| 117 | The evidence list was crowding out the page it was meant to support. On entities with dense findings — Rosneft's curated summary carries 15 claims averaging nearly four citation chips each — the AI summary card ran two-plus screens deep, and on curated examples it does so automatically, before the user has asked for anything. The Evidence section now starts collapsed to a three-claim preview with a soft fade and one clear call to action ("Show all 15 evidence statements", styled in the citation-chip palette so it reads as "more of these live here"), folding back via a "Collapse evidence" text link — the same preview-plus-count disclosure pattern the results page already uses for possible duplicates and sanction notices. Two things deliberately never hide. First, the grounding promise: the card's subtitle says every statement links to its source, so the header now states the full tally ("15 statements, cited to 8 sources") in both states and the preview keeps real claims with live citation chips — a bare disclosure toggle would have made the promise invisible exactly when it matters. Second, the analyst control plane: when sign-off is active the default collapse is disabled outright, because folding the list would hide accept/dispute controls whose state is embedded in the PDF record; the collapsible test is derived per render, so regenerating live from a curated example auto-expands. Lists of five claims or fewer never collapse — the CTA would save no space. The rules are pure functions in `lib/evidenceDisclosure.ts` (the `signalKind.ts` pattern: testable logic out of the 600-line component), expansion is tracked as `evidence_expand` so the threshold can be tuned on real use, gaps and limitations stay always-visible, and the PDF/Markdown exports are untouched — the analyst's record always carries the full list. 6 new tests. Commit `85dc8c4`. |
| 118 | Export-control topics classified at last. `export.control`, `export.control.linked` and `export.risk` sat inside `_RISK_TOPICS` — so `us_bis_mieu`'s 13 topic-bearing entities, every one carrying only `export.control`, were fetched by search — while `risk.py` classified none of them: the hit card rendered the topics in its summary line and **no risk chip at all**, the `sanction.control` failure one family over. The Phase 98 pattern reapplied: `classify_export_topics()` splits control / linked / risk / unknown once in `risk.py` (exact set membership before the prefix test, because `export.control.linked` startswith `export.control`), and three new codes — `EXPORT_CONTROLLED` (high), `EXPORT_CONTROL_LINKED` (medium), `EXPORT_RISK` (medium; upstream's own label is "Trade risk") — fire from all three ladders with `RELATED_*` variants, statement-scoped dedup, and label/chip/badge entries on every surface down to the PDF report's `_CHECKS_CLEAR` boilerplate. Deliberately **no suppression** within the family: model.json (checked 2026-08-20) declares no superset relationship among the export topics, unlike `sanction.linked` ⊃ `sanction.control` — and the export-controlled rung outranks plain sanction adjacency in the most-severe-first order, because it is a restriction on the related party itself. `openaleph_check._WATCHLIST_TOPICS` grows the three topics, keeping its "exactly what the ladder can act on" invariant — export-controlled related entities are now retrieved at all. The part that closes the whole class rather than one family: `risk._INFORMATIONAL_TOPICS` is an explicit 18-topic allowlist (`poi`, `wanted`, `role.oligarch`, the `crime.*` family, `invest.*`, `mare.*`, `reg.*`, `corp.disqual` — each a decision recorded on the ticket, not a default), and `test_every_risk_topic_is_classified_or_allowlisted` asserts `_RISK_TOPICS` equals classified families ∪ allowlist exactly — so the next topic family OpenSanctions publishes is a build failure, not a blog-post discovery. 13 new tests. Commit `931919b`. |
| 119 | The "31× more RELATED_SANCTIONED than OpenSanctions" reading, resolved by measuring before mending. Fresh production diagnostics on Rosneft decomposed the 27 OpenAleph copies: only ~6 were the hypothesised per-collection replication (Igor Sechin ×4 across the Japan/OFAC/UK/Ukraine lists; Syzran Refinery ×2 as duplicate records *inside* Swiss SECO), while **19 — the dominant term — were legal-form false positives**: distinct RN subsidiaries each "matching" a Canadian-list record whose entire name is «Общество с ограниченной ответственностью» — "Limited Liability Company" in Russian — at 0.880–0.889, every point of it boilerplate similarity. (The 11 debarment signals were honest distinct findings and are untouched.) Two changes, one per failure mode. `_collapse_collections()` keeps one signal per `(code, subject_statement_id)` — survivor by (confidence, score), every copy preserved in `evidence.collections`, `collection_count` counting distinct collections, corroboration unioned across copies since one list may publish the birth year another lacks — with multi-collection summaries reading "listed across N OpenAleph collections", deliberately never "corroborated" (aggregation copies inside one instance are not independent sources). The collapse never crosses `source_id`, so the OS+OA duplicate policy stands; and because `/signalstats` counts post-dedup inside `_merge_signals`, the instrument reports the collapsed number automatically — display and counter agree by construction, which was the ticket's second question. And `names.org_name_residue()` + a both-sides entity match guard: a name that erodes to nothing once legal-form tokens are stripped (rigour's `remove_org_types` — `org_comparable_name` can't do this job, it *replaces* the form with a class token) cannot be meaningfully matched, killing all 19 — the empty-residue rule from the distinctive-token ticket, shipped early on the surface where production data proved it live. Directory-collection down-ranking for persons deliberately deferred until post-collapse data shows it is still needed. 8 new tests. Commit `f7ff4f7`. |
| 120 | The distinctive-token scorer — the false positives no threshold could kill, killed by shape. Every collision PR #86's 0.93 cut left behind shares one anatomy (generic tokens match, distinctive token differs: CASTROL↔COSCO at 0.92 on shared filler alone), and the cut cost two adjudicated true matches at 0.877–0.878, leaving LVMH with no offshore-leaks signal at all. `names.distinctive_token_agreement()` — a sibling predicate, with `name_similarity`'s recall-only contract untouched — strips legal forms (`org_name_residue`), joins acronym runs torn by the stripping and drops stranded single letters (the harness's first pass caught 'Gulf International Bank B.S.C' being dropped against its own dotted twin), discards corpus-measured filler ('energy' and 'bank' deliberately kept — dropping them re-admits measured collisions), demands identical numeric discriminators (HORNSEA 1 ≠ HORNSEA ≠ HORNSEA 2), and requires the shorter residue to agree token-by-token with the longer (exact, or one edit for tokens ≥4 chars — 'U.K.' vs 'S.K.' is a different acronym, not a typo). `icij_check` applies it even to ICIJ `match: true` results (ICIJ rated the ENERGEN/BIOGAS collision 90/100), to entity targets and to person-kind targets whose name carries a legal form — corporate officers filed as persons; real personal names bypass, every token distinctive. `_MIN_NAME_SIM` returns 0.93 → 0.87, recovering both named losses. Measured, not asserted: `scripts/eval_icij_distinctive.py` rebuilds the PR #86 corpus reproducibly (14 subjects / 1,140 targets / 29,337 scored pairs; the original harness was never committed) with thresholds read only off the production-shaped pool — the PR #86 lesson, applied. Production pool: precision 69%→≥80% and recall 75%→100% under the worst polarity of every judgement call — and the gate killed two collisions sitting *above* the old cut and still shipping (WIGMORE 1↔WIGMORE 0.9375, PRACTICE PLUS↔PRACTICE PLAN 0.9444), which no threshold could have removed. The surfaces replay sizes the follow-up with data: across cross_check/OpenAleph evidence in the same lookups the gate would drop exactly the 20 Rosneft legal-form matches Phase 119 found — 19 already killed there, 1 OpenSanctions-side remaining — and nothing else. Adjudication CSV + eval doc committed; the stale ICIJ section of risk-signals.md (0.45 Jaccard, batches of 10) rewritten. 9 new tests. Commit `c5e6002`. |
| 116 | The exported report was the last surface still presenting the pre-Phase-111 world. The PDF and Markdown downloads — the analyst's defensible record, with per-claim dispositions embedded — ignored `RiskSignal.kind` entirely: an Ørsted or Rosneft report rendered "Outside EU/EEA" and a permitted GLEIF reporting exception under the **Risk signals** heading, at the same visual weight as a sanctions listing, even though the results page, share card and OG image have all split risk findings from structural context since Phases 111/114 — and the `kind` field was put on the backend signal precisely so no rendering surface could diverge on the count. Both report builders now split on `kind` (missing = `risk`, mirroring `lib/signalKind.ts`): context observations move to a **Structural context** block carrying the results page's explainer copy, styled with a slate border rather than the risk ramp's rose/amber — the Phase 105 counter-sanction lesson, applied to print — and a context-only report correctly reads "No risk signals were raised". Labels stop being `code.title()` churn ("Eu High Risk Third Country") and come from `og_image.SIGNAL_STYLE`, the backend's curated map already kept in sync with the frontend's `RISK_PRESENTATION`, with title case only as the fallback for unrecognised future codes. And the "checks applied and returned clear" boilerplate — which still named non-EU/EEA jurisdiction as a risk check and omitted the EU high-risk third-country list — now describes the screen the engine actually runs, from a single `_CHECKS_CLEAR` constant shared by both builders so the next signal-family change has one string to update. 6 new tests pin the split, the curated labels and the boilerplate; the drift itself is the third "hardcoded engine claims" find, so the reporting module joins that checklist. Commit `59a9bcb`. |
| 121 | Monitoring a source is not asking whether it replied. The Ariregister bug (Phase 120's successor, PR #153) returned correct, live Estonian register data — officers, shareholders, beneficial owners — while recording no provenance observation, so `provenance.resolve()` fell back to `stub` and the UI stamped "Placeholder data" on it. Any weekly health check built the obvious way, asking each adapter whether it returned something, would have gone green every week for as long as the bug lived: OpenCheck's output is a claim about provenance as much as about content, so a source can be entirely functional and still mislead. Phase A of the source-health work therefore gives every one of the 39 registry adapters a **probe** — a known-good local identifier rather than a query, since most adapters are identifier-entry-only — run inside a `provenance.recording()` scope, asserting the **resolved liveness** alongside reachability, emptiness and declared fields. The weekly sweep (`scripts/source_health.py`, Mondays 07:30 UTC between the drift checks and the securities refresh) runs against a scratch data root with cache reads disabled, so a probe cannot be graded on OpenCheck's own cache and the single retry cannot read back the failed attempt's write; it reports ids, statuses and field *names* only, credentials redacted, matching the `degraded_sources` privacy rule. Two guards move into the PR gate rather than waiting a week: `PROBES`/`REGISTRY` parity, so "green" can never quietly mean "green for the sources someone remembered", and an AST check that any adapter building its own `httpx` client records provenance in the same function — which found a second, latent instance of the same defect in `fetch_timeline_data`'s X-Road path and fixed it. The first full run also found a live one: Lithuania's JAR interface returns HTTP 403 and the adapter falls back to a bundle carrying the GLEIF name with every register field null, which from the outside reads as a successful lookup. A second pass closes both provenance gaps the first left standing — `eiti_soe` was *over*-claiming `live` on the strength of its live payment rows while its central assertion came from a committed index, the mirror image of the Ariregister defect, and `bce_belgium` recorded nothing at all — and adds the **GLEIF dispatch-drift** check: every national register is entered via a key derived from the GLEIF anchor, so a renamed registration-authority code or a change in a registrar's number formatting stops a source being dispatched *at all* while its own probe stays green, a failure of the join that no per-source probe can see. The normalisers it exercises are load-bearing — GLEIF returns Norway's number as `'923 609 016'` and Croatia's zero-padded to nine digits. Filling `expect_fields` from the first run's observed shapes then proved the point twice over: asserting `legal_name` fails, because adapters echo the GLEIF name in as a display fallback, which is precisely what lets a 403 read as a successful lookup; and two further probe subjects lifted from test fixtures turned out to be a dead Latvian regcode and a dissolved Slovak branch. A probe identifier taken from a test file is not a verified subject. A third pass completes the picture: BODS statement counts are now recorded per source and diffed against the previous run's artifact, reporting a **collapse** rather than movement, so a source that still answers and still resolves `live` but has quietly stopped carrying ownership edges is caught — the earliest machine-detectable signal of an access change, and the only assertion that would see Estonia's postponed legitimate-interest switch land. Counting by interest *histogram* rather than a single beneficial-ownership bucket was itself forced by the data: Estonia's ten beneficial owners map to `otherInfluenceOrControl`, so a bucket watching `beneficialOwnershipOrControl` reported zero for a source carrying plenty. Snapshot and curated sources gain an age check against the date the index itself declares — never a file mtime — because their failure mode is silent ageing rather than an error; and dispatch-drift coverage goes from 9 to 20 of 23, each anchor verified by running the adapter's own normaliser against the live GLEIF record. The first fully-credentialed run then closed the loop on the Lithuanian finding: JAR passes from a residential connection and 403s from a datacentre, so the register was never broken — but the adapter's response to being refused was to return a card carrying the GLEIF legal name, every register field null and `is_stub: False`, which no consumer could distinguish from a successful lookup of a company the register says little about. Adapters now have the same recorder shape provenance has: a per-lookup `degradation` scope they can write into from inside a dispatch task, collected into the existing `degraded_sources` alongside the derived screens, and the bundle is marked `register_unavailable` so the UI, the exports and the sweep can all tell. The sweep reports it as degraded rather than failed, because a monitor that is permanently red for something that is not broken stops being read. The same distinction reaches the verdict sentence: a screen that did not run makes an empty result impossible to mistake for a clean one, but a *source* that did not answer is a coverage gap rather than a screening gap, and saying "not a clean screen" of the Lithuanian register would over-claim on one while under-explaining the other. Commits `efe0da1`, `231ab81`, `131ff78`, `30a8ec3`, `ee6b54d`, `5c6234f`. |

| 122 | Design v2, phases A+B — the primitives, and the answer first. The audit behind the [Design v2 refresh](https://app.notion.com/p/3c37f3dc2928801eae3ad4ccba40ae15) ticket counted 61 hardcoded hex values, 520 arbitrary `text-[NNpx]` classes across 26 files, nine button styles, twelve chip families and eight eyebrow variants — the design system had held on colour and font but not on components, so the same meaning looked different depending on which file you met it in. **A:** `components/ui/` — `Button` (five variants, one focus ring, a 44px default target, and a hover that no longer turns a blue button grey), `Chip` (six tones chosen by what the chip *asserts*, with the confidence level finally spoken to screen readers rather than left to a `aria-hidden` glyph), `SectionLabel`/`SectionHeading`, and an `Icon` set that replaces the chevron pasted five times and the `⬇ ↓ → ⚠️ ↗ ‹ › ▸ ▾` text glyphs. New tokens: `oo.soft`/`oo.softBorder` (the `#eef1fb`/`#cfd6f5` pair, 24 hardcoded uses), semantic `warn`/`ok`/`info` tiers, an eight-step named type scale, and `oo.node.teal` for the fourth check mode. The graph palette is tokenised and moved onto the brand node tier — ownership to `oo.node.blue`, role to `oo.node.purple`, so the network mode's accent and the ownership edge are now literally the same value, with darker label colours where 4.5:1 needed them; `#1565c0`, which had been doing double duty as "ownership edge" *and* "selected" across five components, is retired in favour of `oo.blue`, and `reporting/diagram.py` moves with it so exported PDFs cannot disagree with the screen. **B:** the report opens with the subject. `opencheck/verdict.py` builds one deterministic sentence from the signals and degradations already on the `risk_signals` event — a template, not a model call, so the page, the PDF, the share card and the API cannot drift and a lookup costs no extra tokens; it states what the records contain and never grades the company, and it refuses to read as a clean screen when a check did not run. `VerdictStrip` renders it above the evidence with what-we-found and coverage side by side, `DegradedScreensNotice` moves above the AI summary (it used to render after it, so a reader reached a conclusion before learning which screens had not run), the hero and the four-tab search panel come off the results page, and the header nav is constant across every view instead of collapsing to "← Back". 28 new tests. Commit `a8ba5e7`. |

| 123 | Design v2, phases C+D — modes as the structure, and rows that say what a source found. **C:** the three mode cards in the middle of the page become the report's tablist. The subject and verdict stay put across a switch; the choice goes into `?mode=` and the document title (QuickCheck keeps the bare `NAME - OpenCheck` form byte-for-byte, since that string is the server-rendered `/entity` template and search engines have it); focus moves into the new panel, which v1 never did — switching mode unmounted most of the page and dropped focus to `<body>`. Left/Right move between tabs on the WAI-ARIA pattern with a roving tabindex, and each tab carries the `oo.node.*` accent that already names its badge. **Climate & ESG becomes the fourth tab**: it had rendered as a section inside QuickCheck, reachable by scrolling and by nothing else, and its empty state now says an absence of published records is not a finding about emissions. `/about` describes all four — BackgroundCheck had shipped undocumented. **D:** `SourceHit.finding` — one plain-English sentence per source, built by the adapter from fields it already parsed (`opencheck/findings.py`, ten rules in the module docstring: one sentence, never name the source, lead with what changes a decision, no value the adapter did not parse, numbers exactly as filed, absence stated in the same voice as presence, no risk judgement, degrade rather than emit a slot marker). Seven adapters have templates; the row falls back `finding → summary → nothing`, so the ~30 without one look exactly like v1. Writing them found six places where the sentence in the design mockup was not supportable by the data — GLEIF's Open Ownership extract carries no ownership percentages or parent names, OpenSanctions' dataset slugs cannot be expanded into "EU, UK and US regimes" without inference, OpenCorporates parses `gb` and not "England and Wales" — and in each the template says less rather than more. The API chip comes off the subject's identity row and the Neo4j CSV icons off the homepage example cards: developer affordances on the two surfaces a first-time analyst meets first, both still available where someone looking for them would look. **The GLEIF correction:** the first pass put the template on `bods_gleif`, which is the Open Ownership Parquet extract — deliberately unregistered, serving only the three curated bulk-BODS examples — so `gleif`, the source every live lookup actually shows, had no sentence at all. `finding_gleif()` is written against the live `GleifAdapter.fetch` bundle, which does carry parent names (each parent endpoint returns the parent's own Level 1 record), reporting-exception reason codes and a direct-children count. Its vocabulary is pinned by the standard rather than by what reads well: GLEIF Level 2 is **accounting consolidation**, not shareholding, so every producible sentence says "consolidated by" and three parametrized guards over all twenty of them fail the build on a `%`, on `owns|held|shareholder|stake|equity|majority`, or on a missing `consolidat`. A reporting exception is worded as the permitted filing the LEI ROC policy defines, not as a refusal to disclose, and the five reasons `NON_PUBLIC` absorbed in Reporting Exceptions Format 2.1 read the same as the code that replaced them. `finding_bods_gleif` was re-worded to the identical vocabulary so the two paths cannot describe one relationship with two verbs; its children lose the word "direct" unless every child interest is filed as such, since the Parquet relationship table holds ultimate links too and only the live path counts GLEIF's own `/direct-children` endpoint. 221 new tests. Commits `fbca1ba`, `54c98ca`. |

| 124 | Design v2, phases E–H — one diagram, one vocabulary, one honest progress bar, and a lint that keeps them. **E:** three renderings of the same statements could be on screen at once — the explorer's Split/Graph/Tree switch, BODSGraph's own "View as table" inside the Graph pane, and SubsidiaryNetwork's children list under both — so Split mode with the table on showed two different tables of one statement set side by side plus a third list below. Now one canvas with `BodsTree` as its text equivalent, a disclosure under the diagram rather than a mode you can land in *instead of* it; `BodsRelationshipTable` retired, with the two things it did better carried across (risk-signal labels per party, and unconnected parties marked as such rather than rendering as ultimate parents). The legend is **generated from the values the canvas draws**: `SIGNAL_STYLE` defines thirty badges and the hand-written legend covered three, the rest explained only by a `title` carrying the raw code. `SIGNAL_STYLE` plus a new `EDGE_STYLE` / `NODE_MARK` moved to `lib/graphStyle.ts` — out of the component, so the legend can read them without importing Cytoscape and the logic-only suite can pin them; `buildGraphLegend` scopes the legend to the graph in front of the reader, worst severity first. **F: `title=` is gone from the app.** All 22 non-`BtsCard` occurrences: 18 were the sole carrier of something substantive — the GLEIF accounting-consolidation caveat on the one badge most likely to be misread as ownership, what a match percentage measures, a licence's plain-English terms, a 240-character paragraph a native tooltip truncates anyway — and the rest duplicated a visible label, one of them the *worse* of two strings. `ui/Explain.tsx` replaces them; the three most decision-relevant are visible rather than `sr-only`, since hiding a sentence from sighted users reproduces the original bug with the audiences swapped. The ●◐○ confidence glyphs gained a visible legend (their meaning existed in one place and was only ever emitted `sr-only`), and `lib/vocab.ts` fixed the collisions the audit found were concrete: results not hits, one verb per action replacing Search/Look up/Screen person/Run background check, source display names replacing `via companies_house` and a `.join(" and ")` that built English out of raw slugs, FollowTheMoney topic ids translated, acronyms expanded on first use. **G:** the loading grid was a lie with an expired excuse — its docstring's premise stopped being true in Phase 47 — driving every chip off `setTimeout` with a random shuffle, rendering the whole registry rather than the dispatched sources, and announcing "Queried 39 sources" through `role="status"` on a timer for a lookup that had not returned. It now renders `sources_applicable` / `source_started` / `source_completed` / `source_error` and nothing else, with `total` null rather than 0 while unknown and failures counted separately. `SecuritiesSection`'s catch swallowed its rejection with no state write, so a 500 silently converted the always-visible sanctioned-securities banner into an absence; both it and `SubsidiaryNetwork` now report to a notice that is deliberately **not** `degraded_sources`, because that list arrives on the same event as the signals and the backend-built verdict and the three are provably consistent. `InvitationStrip` extracted — two copies, six shared hex literals, and the focus-restoration fix had landed in only one of them. **H:** a design-system lint on the OKF-drift pattern, a **ratchet rather than a ban** because a lint that fails on 477 existing violations is a lint that gets disabled: a file may never carry more raw hex or more `text-[NNpx]` than its baseline, a new file may carry none, and an improvement must lock itself in. Writing it immediately caught a duplication phase E had introduced — the edge colours in `graphStyle.ts` *and* still in the Cytoscape stylesheet — which is exactly what it is for. An adversarial review then found eleven defects across the four phases, including a `role="img"` name still pointing at the deleted table view, a legend that omitted `possiblySame` edges, a panel-error notice that outlived its own subject, and a `sec_edgar` path that could leave a finished lookup reading "11 of 12 sources answered". 73 new frontend tests, 3 new backend. Commits `b92224a`, `9e6703b`, `8b13d7d`, `b453843`, `e37d3e2`. |

| 125 | Design v2 — finishing what F and G left open, and a lint that argued back. **`MatchConfidenceChip`**, plus the finding that two of the four "confidence chips" the audit wanted merged are not confidence at all. `ClusterGroup` and `NzAssociations` both grade a *name match* — a person-pair comparison and an address tier — in different colours with different words for the same level; `SubsidiaryNetwork`'s `RelationBadge` renders a relation kind, and `RiskChip`'s glyphs are corroboration, already unified with `ui/Chip` in Phase 124. The distinction is not pedantry: `CONFIDENCE_LABEL` reads "Corroborated by two or more sources", and putting that beside a single-source name match would be false in the one place the product is most careful, since a name match is explicitly not an identity claim. Match strength therefore gets its own words, never borrows ●◐○, and every level says what it rests on ("only the name agrees — not an identity match"), which neither old chip said anywhere. **The vocabulary pass finished:** "person-capable source" named an adapter capability flag at five sites; "frontier companies" / "node cap reached" / "frontier exhausted" are graph internals in sentences that otherwise avoid them, two of them announced through `aria-live`; the narrative panel's "N statements" became "N claims" (the field is named `claims`, and BODS statements render on the same page); `EsgPanel` stopped using a raw `source_id` as a card heading; and `stub` reads as "placeholder data", which `LivenessBadge` already called the same state. **`BANNED_SYNONYMS` is enforced — and enforcing it proved the list wrong.** It banned single words, and each turned out to have legitimate uses the moment a lint was aimed at them: "an empty result here is not a clean screen", "a fast screen of the subject", the SSE event named `"hit"`, `Liveness = "stub"`, `min-h-screen` in a Tailwind class. The first attempt reported 90 violations in `App.tsx` alone, all false; banning the word would have forced rewrites of correct English to satisfy a rule aimed at something else. What actually regressed was a *label* — a button saying "Screen person" beside another saying "Run background check" for one action, an empty state saying "No hits." forty lines from "3 results" — so the list is now exact phrases. Three matcher attempts are recorded in the code so nobody retries the first two: word boundaries over whole files flag `bucket.hits.length`; restricting to quoted strings is worse, because an apostrophe in JSX text pairs with the next one and swallows the code between. It uses TypeScript's own parser, walking string literals and JSX text minus anything inside a `className` — zero false positives, and it caught this phase's own baseline improvement. **`Described` deleted unused:** every short case wanted inline text appended to the control's accessible name, and the one place that genuinely needs `aria-describedby` already does it directly; an unused primitive invites the wrong pattern to be adopted because it exists. 3 new frontend tests. Commit `cc44912`. |

| 126 | Design v2 — the report becomes the design, not just the information architecture. Raised from production: *"a combination of old v1 and new v2 elements"*, *"some things are showing twice"*. Both correct. Phases A–H had converted the **information architecture** — modes as tabs, rows leading with a finding, one graph surface, a lint — and no phase's scope said "convert the report body to the mockup", so none did; the write-ups described what changed and never what had not. **Two defects first:** `SubjectCard` kept its own risk summary, which `VerdictStrip` has replicated since Phase 122, so every report stacked two verdicts; and Coverage read **"13 of 12 sources answered"** above "Every applicable source answered", because it counted the completed set, which includes the GLEIF anchor — announced before `sources_applicable` and never in that list. **Part 1, the visual system:** the report is one card whose sections are bands, as designed, instead of detached cards on grey with the tab strip connecting to nothing; section titles are 16px Bitter `<h2>` via `SectionHeading`, which Phase 122 built and nothing had ever adopted, rather than a 12px uppercase eyebrow in a `<p>`; subject and verdict share one card; structural context folds into Risk signals as a captioned sub-block, so the confidence legend prints once. Two things surfaced doing it: QuickCheck's narrative, risk signals and both identity sections rendered **outside** `#panel-quick`, so the tab did not contain what it claimed and a screen-reader user navigating the tabpanel never reached the risk signals; and `App.tsx` carried its own local `SectionLabel` shadowing the design-system one, which is why the Phase 122 primitive never took effect. **Part 2, the source rows:** three equal-weight chips labelled by what they hold — the party count, changes over time, a neutral `Data` disclosure — replacing a full-width blue call-to-action and two 11px mono links; the drawer is a menu of tiles that open things rather than the Cytoscape canvas and two raw JSON dumps inline under `BODS · N statements` / `Raw source payload`; a failed source is amber with the consequence spelled out rather than a red raw error string; freshness moved inline beside the source name, and a live source now says "Checked today" — `LivenessBadge` returned null for live, which made live indistinguishable from a badge that failed to render and left the column unscannable, against this project's own rule that absence is stated in the same voice as presence. **An adversarial pass then found thirteen defects**, one high: the timeline chip rendered twice because part 2 added it to the action group without removing the old render, and `answeredCount` still counted an errored source as answered while part 2 rewrote its card to say "Did not answer" — the strip could read "13 of 13 sources answered" above a card badged otherwise. Also: a drawer that could hide its own controls, an empty card on the homepage, two sections missed by the band sweep still drawing cards inside the card, a label restyled to look like a heading while staying a `<span>`, and a "+N more below" pointing at a jump that no longer exists. 12 new frontend tests. Commits `4f09609`, `e07f28d`, `2368c13`, `00d2f6b`. |
| 127 | Stephen checked the deployed report against the v2 design and found eight things, four of them structural rather than cosmetic. The risk chips in the verdict strip's "What we found" column were inert — `interactive={false}` — so the first three signals a reader meets were the only ones that would not open, the same chip behaving differently depending on how far down the page it sat. The search panel was still above every result: the tab bar stayed as a "landmark" while only the input panels collapsed, which is the v1 page's opening move, search first and answer second. "7 parties" on the diagram chip was computed from `breakdown.entities` alone, so a Companies House row labelled seven opened onto eleven nodes — it says "7 entities · 4 people" now, and the backend counts `persons` alongside entities so the figure exists before a source is deepened. And the **ownership-network column never shipped**: the strip had been two columns for four phases, leaving the mode tabs as the only invitation into FullCheck, and a tab strip does not say what is behind it. New `graph_shape` on the `risk_signals` event carries the size of the graph this check actually mapped, never the GLEIF subsidiary total or anything FullCheck would go on to find; `lib/graphShape.ts` decides when there is enough to render — one company is not a network, neither are two unconnected records, and depth is spoken only when `COMPLEX_OWNERSHIP_LAYERS` measured a chain. **The other four were sections still wearing v1.** "AI summary" + a Beta badge named the machinery and graded it, which is two statements about OpenCheck where the reader wanted one about the company; it is "Summary" with "Written by AI, cited to sources" beside it, and the mockup's "Mark as reviewed" got somewhere real to write — `DispositionRecord.reviewed`, deliberately **not** derived from the per-claim dispositions, because "I have read this" is weaker than "I accept every sentence in it" and collapsing them would let one click stand in an audit trail as claim-by-claim agreement. Download data traded a `<select>` for eleven format chips and moved licensing from small print under the button into a "You can reuse this" card beside it; `csv` and `cypher` — already inside other bundles — are now served on their own. OpenAleph's archive matches were a second white card stacked under the first, titled by a hand-rolled uppercase `<p>`; they are a band inside the source card. TED's five bordered mini-cards became a divided list. **An adversarial pass then found ten defects**, one severe: `_graph_shape` deduplicated by `statementId`, and since every mapper derives ids as `_stable_id(source_id, kind, local_id)` that collapsed nothing at all — a graph of two companies was advertised as three on nearly every lookup. Records now join on a shared published identifier, transitively, and never on names: `possibly_same_entities` exists to keep name-only pairs out of the graph, and that cannot be quietly reversed because it would make a number smaller. Also: a review survived a regenerate and stamped `reviewed_at` on text nobody read; one click on "Mark as reviewed" before hydration landed deleted the stored disposition sheet; the format picker claimed `role="radiogroup"` without arrow keys or a roving tabindex, which is worse than the `<select>` it replaced; and the network column put an `<h2>` inside a `<button>`. Commits `4b1a6d2`, `abc58c6`, `7e34c01`. |
| 128 | The rest of the report becomes the design. Three of the four tabs still connected to nothing: FullCheck drew a blue blurb strip, a white graph card and a grey subsidiary card as separate objects on the page; BackgroundCheck drew a violet strip and a white empty-state and left everything else uncarded; Climate & ESG drew a rule-flanked divider above a card whose own header repeated the same title and offered to collapse itself. Each tabpanel now owns a `PanelCard` of `PanelSection` bands, and the three self-written blurb strips become one `ModeBlurb` reading the strings that had been sitting on `MODE_TABS` since Phase 122, rendered nowhere — three chances to disagree with the tab above them, now one. **"Is this the right company?"** replaces two collapsibles: cross-source identifiers and possibly-same pairs are the same enquiry from two directions, and splitting them made a reader open two boxes to answer one question with the reassuring half and the doubtful half in separate containers where neither qualified the other. It is no longer collapsed either — that is the question a reader looking at the identifier badge is already asking. **"Share and export"** replaces three entry points for one intention: "Copy share link" on the subject, an "Export" menu in the summary card header halfway down the page under a heading about something else, and the Download data button. The menu lived there because the PDF and Markdown routes post that card's narrative and dispositions, so the payload now travels up and the handlers live on App. `SecuritiesSection` and `MeipSignpost` stop hand-rolling the band class string, the heading classes and their own split-heads. Earlier in the round: the licensing panel moved out of a 300px side column that real data broke on sight — "Nigeria CAC — Persons with Significant Control register" wrapped to seven lines and its licence pill overflowed — and the mode tabs lost the 24px gap under them. **An adversarial pass found ten defects.** Two mattered: a failed export put a sentence in the subject card's `shrink-0` control column, which at 375px crushed the company name to zero width, one character per line; and `?mode=` deep links had never worked, because the lookup mutation's own reset to QuickCheck runs asynchronously and landed after the handler that read the URL — the requested mode goes into the lookup now rather than beside it. Also: export state surviving into the next entity (a stale PDF alert on a new subject, and a payload that would have embedded one company's summary in another's report), an aside promising "everything here is in the table too" beside a failed graph, and `sourceList` inventing the brand name "Openfigi". 57 arbitrary type sizes and 4 raw hex values retired. Commits `b0ecfb2`, `08be983`, `f8d91cb`, `8c7d358`. |

Test suite: 3,430 backend tests pass (18 skipped — incl. the opt-in live smoke tier, 5 xfailed). Frontend type-checks clean and the vitest suite passes (318 tests across 28 files, incl. the changelog parser, the expansion frontier + risk-signal helpers, cross-source signal scoping, subsidiary edge-rationalisation and Time Machine data-shaping tests); the production build splits the Cytoscape graph into its own on-demand chunk.
